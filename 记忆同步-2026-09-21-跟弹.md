# 记忆同步 · 2026-09-21 · 跟弹（判定质量）

> 接着 `记忆同步-2026-09-20-跟弹.md` 的「明天从这里继续」第 1、2 条做的。
> 一句话：**那 3 处「判错」不是用户弹错，是起音快照抓早了。**
> 真机录音：对 28／错 3／测不准 9 → **对 36／错 1／测不准 3**。

## 1. 先看证据：那三处「判错」其实都弹对了

把同一个音从起音前到起音后**用产品页自己的估计器**滑窗量一遍（170ms 窗，40ms 一步）：

| 位置 | 谱面要的 | 起音快照 | 整段实测 |
|---|---|---|---|
| #6 | D4（2弦3品） | 283Hz = −65 音分 → 判错 | 287~302Hz，**就是 D4** |
| #14 | E4（1弦0品） | 318Hz = −60 音分 → 判错 | 324~340Hz，**就是 E4** |
| #19 | A3（3弦2品） | 212Hz = −65 音分 → 判错 | 起音后 40ms 起稳定在 218Hz，**就是 A3** |

原因：起音那一瞬新音的谐波还没立起来、旧音还在响，快照读到的是「交接中」的谱。
**错的不是演奏，是抓早了。**

同一件事在新加的合成回归里量得更清楚（干净信号）：
真弹 D4 → 起音快照读 **+70 音分**；同一条音频用「判定这一刻的平窗」只读 **+5 音分**。
D4 弹成 D#4 → 平窗读 +80 音分（能分开），快照读 +70 音分（分不开）。
**起音快照的偏差能到半个半音。**

## 2. 今天改了什么

1. **判错之前先复核**（`frontend/js/follow-score.js`）：判定这一刻（起音后约 90ms）用同一个估计器
   再量一次**平窗**（170ms、不做抬头过滤 —— 这时新音已经站稳），落在 ±45 音分内就判过。
   只往「判过」的方向兜底：判错是重判据，两次测量都不同意才判错；真弹成别的音，复核也会给出那个音。
   今天这一条救回了 10 个音（复核值全在 ±35 音分，正是正确演奏该有的散布）。
2. **导出记录变全**：每个音多记 弦/品、上一音、最强邻音、能量比(=期望/邻音)、是否贴搜索边界、
   复核值、该弦当时的音准校正量。这些是调参唯一的数据来源 —— 少了「输给谁、差多少」只能靠猜。
3. **新增 `test/test-follow-page.mjs`**：产品页判定的**合成回归**。
   以前 test-live / test-detect 跑的都是调试图（main.js），产品页判定只能靠真机录音验。
   现在 4 个场景：换品弹对 / 弹成高一品 / 弹成低一品 / 只弹一下。
4. `test/test-follow-real.mjs` 加 `VC_DUMP=1`（逐音打证据）。

### 试过、但**不采用**的（别重走）

| 做法 | 真机结果 | 为什么不采用 |
|---|---|---|
| 关掉起音抬头过滤 | 对 34／错 0／测不准 6 | 没有变好 |
| 判定时重新取快照当主判据 | 对 36／错 1／测不准 3，但合成里「弹错一品」变成「测不准」 | 错音被放走 |
| 同弦换品兜底（离目标比离上一音更近就算过） | 对 38／错 0／测不准 2 | **合成回归场景 2 里，一个干干净净的 D#4（谱面要 D4）被它放过** —— 作业评估不能把错的悄悄放过 |

## 3. 成绩单（同一段 30 秒真机录音）

| 版本 | 对 | 错 | 测不准 |
|---|---|---|---|
| 9-20 最早 | 4~5 | 23~36 | — |
| 9-20 收工 | 28 | 3 | 9 |
| **今天** | **36** | **1** | **3** |

剩下的 1 错（A3 第5小节）+ 3 测不准（A3/C4/D4，复核 +55／+65／−70 音分）都是
**同一根弦的量到了半音中间**：这几个音在整段录音里怎么都由不出一个稳定音高，
连平窗复核也在边界上。要么判过（有放过错音的风险），要么提示「这一处没听清，重弹」。

## 4. 两个已知缺口，都要你拍板

1. **一品之差会被放过**：谱面要 D4、弹成 C#4（低一品）判「对」。
   根因是起音快照系统性偏高（真弹 D4 读 +70 音分、C#4 读 −50 音分），
   而「没有明确反证就判过」的线正好在 65~100 音分之间。平窗能分开（+5 / −80）。
   → **要你定**：判错宁可慢 100ms（只在本来要判错时再等一次复量），还是宁可放过一品之差？
   （`test/test-follow-page.mjs` 场景 3 现在按「已知缺口」标着，补上时会自己提示。）
2. **起弹第一下会被吃掉**：`follow-score.js` 里那句遗留的对齐逻辑（`alignOffsetSec`）
   会把**第一个起音**当对齐参考、不判它。合成回归场景 4 钉住了这个行为。
   改掉会让整段「起音 ↔ 音符」位移一位 —— 那段真机录音现在能对上，
   有可能正是因为开头那一下是多出来的声音。
   → **要你定**：那段录音开头是不是多了一下？（是 → 保持；不是 → 这是 bug，得改。）

## 5. 怎么跑

```
cd /d E:\GuitarFollowLab
node test/test-product-page.mjs     :: 产品页能不能加载（DOM 桩，不开浏览器）
node test/test-follow-page.mjs      :: 产品页判定的合成回归（今天新加）
node test/test-live.mjs             :: 调试图那条链路
node test/test-detect.mjs           :: 能力边界（连弹多快会崩）
node test/test-outsiders.mjs        :: 和弦外音
node test/probe-settle.mjs sound_data/f32/hey_jude.f32 9.28 62
                                    :: 看一个音起音前后的音高怎么走（今天就是靠它定的案）

:: 真机录音只做标定用，别当日常回归（用户明确要求）
set VC_DUMP=1
node test/test-follow-real.mjs sound_data/f32/hey_jude.f32
```

## 6. 下一步（按价值排）

1. 定第 4 节两条 → 「一品之差」那条决定要不要做「判错前再等一次复量」。
2. 3 处「测不准」的处理方式（判过 / 提示重弹）—— 第 3 节的边界就卡在这。
3. 交互：手机上信息条占地方；「要改的地方」点一下跳到那一小节重练（用户 9-20 提的，还没谈完）。

---

## 7. 下半场（同一天，用户回了话之后）

用户答复了两条：①一品之差**必须判出来并告诉他弹错了**；②那段录音**没有多出来的音**，
开头就是 2弦1品 —— 也就是下面这个 bug 是真的。

### 7.1 修掉：第一个音被吃掉
`follow-score.js` 里遗留的对齐逻辑会把**第一个起音**当参考、直接 `return` 不判它。
已去掉。合成回归 `test-follow-page.mjs` 场景 4 钉住了这个修复
（原来钉的是"第一下不该被判"这个错误行为）。

### 7.2 做出一把准得多的尺子（但**还没接上判定**）
`analysis.js` 新增 `estimateF0ByPeaks`：谐波峰 + **抛物线插值**（调音器那一招，取到谱线的百分之几）
+ 多谐波最小二乘 + 弦刚性。旧办法每个谐波只取"最高那根谱线"，而一根谱线在 261Hz 处就是
**39 音分**、在 349Hz 处 29 音分 —— 读数天生是跳着的。

| 量法（你的 30 秒录音，40 个音，按弦校正后） | 中位误差 | 90 分位 | 落在 ±50 音分内 |
|---|---|---|---|
| 旧（estimateF0Near + 起音快照） | 45 音分 | 90 | 53% |
| **新（estimateF0ByPeaks + 判定窗）** | **16 音分** | **36** | **98%** |

量法：`node test/probe-estimator.mjs <onsets.json>`。

### 7.3 为什么**没**把它接到判定上
接上之后，那段录音里有 **9 处被读成"整整低一个半音"**（-88 ~ -125 音分）。
其中至少一处能确定是尺子自己的错：23.3s 那个音谱面要 G3，那段谱里最强谱线就是 199.2Hz(G3)，
尺子却给出 184Hz(F#3) —— 上一个音还在响、两个音差一个全音时，**谐波梳子锁到了两者中间**。
判错会冤枉人，所以判定回到今早那套（那段录音现在是 对35/错1/测不准4）。
9 处的时间点列在 `思路点评-2026-09-21.md` 第 8 节，等用户听一遍确认是"真按低了"还是"量歪了"。

### 7.4 另一件顺手修的
`advanceNote()` 会在最后一个音上收尾出总结，而错音记账排在它后面 ——
**最后一个音的错误进不了总结的"要改的地方"**。已改成先记账再推进。

### 7.5 新增的文件
```
思路点评-2026-09-21.md   对用户《思路.md》的逐条点评（哪条对得上、哪条要改、哪些是阈值问题）
test/probe-estimator.mjs  量"音高估计准不准"，对比几种窗/几种估计器
test/probe-accuracy.mjs   量"不同窗口策略"的散布
test/test-follow-page.mjs 产品页判定的合成回归（场景 2/3 是"一品之差"的产品要求，现在标着已知缺口）
```

---

## 8. 第三轮：**一品之差判出来了**（用户说"你去做"，于是接着推）

### 8.1 找到了那个"整整差一个半音"的根因
不是"锁到两个音中间"，是**选模型时在奖励"丢掉不好拟合的谐波"**：

```
B=0.0004 → 12 个谐波全在，残差 49.7 → f0=194.2Hz (G3, -16音分)  ✓
B=0.0016 → 扔掉 2 个谐波，残差 45.2 → f0=184.1Hz (F#3,-109音分) ✗ 但它"残差更小"，所以被选中
```

弦刚性 B 越大、能扔的谐波越多，残差就越小 —— 典型的过拟合。
**修法：选模型时按"每个被丢掉的谐波罚 20 音分"**（`analysis.js` 里的 `quality`）。

### 8.2 修完之后的数字（都拿产品里那个函数量的）

| 量法（那段 30 秒录音，40 个音，按弦校正后） | 中位 | 90分位 | 最大 | ±50 内 |
|---|---|---|---|---|
| 旧（estimateF0Near + 起音快照） | 45 | 90 | — | 53% |
| 新，修罚分之前 | 16 | 36 | 67 | 98% |
| **新，修罚分之后** | **12** | **32** | **45** | **100%** |

一个半音 = 100 音分，测量散布全部落在 ±50 音分内 —— 这才够判一品之差。

### 8.3 判定接上去了
`follow-score.js` 的判定改成：把**校正后的实测音高归到最近的半音**。
≤50 音分 → 对；归到 ±1/±2 半音 → 错（并说"你弹的是约 X"）；再远或谐波太少 → 测不准。

| 用哪段验 | 结果 |
|---|---|
| 用户那段 30 秒真机录音（他弹的是对的） | **对 40 ／ 错 0 ／ 测不准 0**（原 28/3/9） |
| 合成回归：谱面要 D4、实际弹 D#4 | 判错 ✓ |
| 合成回归：谱面要 D4、实际弹 C#4 | 判错 ✓ |

**"约 X"**：判"不是 D4"很稳；说是哪个邻居用的是宽搜索（±140 音分）的尺子，
会被上一个音和重叠谐波带偏（合成里真弹 D#4 会认成 C#4 附近），所以不把话说死。

### 8.4 新增：真机测试的批量评分入口
```
node test/grade.mjs            :: 跑 sound_data/f32/ 里和谱面对得上的录音，出一张表
node test/grade.mjs --all      :: 全部跑（和弦练习那几段跟这份谱面对不上，数字没意义）
```
输出：每段录音的 起音/对/错/测不准/漏 + 判错的地方 + 逐音明细 JSON（`grade-<时间>.json`）。
**用法：同一段录音跑两次数字应完全一样；改完代码再跑一遍，数字变了就是这次改动的影响。**

### 8.5 还没做的（下一轮）
1. **延迟校准**（下一个做）：外放伴奏会被麦克风收进去，现在还没测/没扣这个延迟；
2. 反复记号展开 + tie：**这份 Hey Jude 谱里 tie 和反复都是 0 个**，做出来也没法验证，
   用户说重复符号先不说；
3. 和弦逐音判定：要先做按弦分带。
（用户明确：纯噪声/说话那个场景先不管 —— 判定要过起音、周期性、谐波三道关，说话基本过不了。）

---

## 9. 第四轮：检查模式（时间层）+ 节拍器

## 10. 第五轮：连续快音（**未解决，账记在这里，别重复走**）

用户实测：两个连续的、比较快的音会被并成一个 —— 第二个音的起音被丢掉，
于是光标停在第二个音等你再弹（"第二个音跟不上"）。

**已确认的要害**：判定前有一道"像不像一根弦在振"的关卡，用的是 YIN 的 clarity。
而 `dsp.js` 里写得很清楚：**YIN 在"上一个音还在响"的混合信号里会直接放弃（返回 0 音高）**。
连续两个音的第二下正好落在这个混合里 → 被判成"不像琴声"丢掉。

**原曲速度下的真实要求**：Hey Jude 76BPM、相邻音最紧 **197ms**（中位 395ms），
所以 200ms 不是边角料，是必须过的线。合成回归里已经有 200/300ms 的场景。

### 试过的五把尺子（都不行，别重复）

| 尺子 | 连续快音（要进来） | 风/拍桌子（要挡掉） | 结论 |
|---|---|---|---|
| 谐噪比（谐波峰/两侧谷） | 1.8 | 1.6 | 分不开 |
| 谱稀疏度（最强的 24 根谱线占比） | 0.09 | 0.16 | 方向还反了 |
| 谱平坦度（几何/算术平均） | 0.85 | 0.71 | 方向反了（两音叠加谱线更密） |
| 局部峰锐度（整谱） | 0.14 | 0.13 | 分不开 |
| **差分谱峰锐度（新加进来的那部分）** | **0.45~0.50** | **0.19~0.32** | **纸面上分得开**，但接进判定后真拨弦也被毙（判了 0 个音） |

最后一把之所以纸面能分：整谱上"两个音叠在一起"和噪声长得一样（前四把都栽在这里），
而"**新加进来的那部分**是不是一串谐波"这件事差别很清楚。
接进去失效的原因：`novelSpectrum` 减的是**缓慢自适应的本底**，把起音那一下抹平了 ——
在起音后 90ms 量出来的差分谱，已经不是"起音那一瞬新增的东西"了。

### 下一招（有把握的方向）
不要再用"最近一帧 vs 上一帧"，改成**以起音采样点为中心、前后各取一小段做差**：
起音前 30ms 的窗 vs 起音后 30ms 的窗，差值谱才是"这一下新加的东西"。
这需要把起音那一刻的采样位置记下来（现在只记了时间），音频要有环形缓存。
有了它，差分谱峰锐度那把尺子才有意义。

### 9.1 检查模式：先报"偏差多少毫秒"，不和音准合成一个对错
`follow-score.js` 里加了时间层，只在 `mode=tempo` 生效：

1. **按时间对号**：练习模式是"下一个还没判过的音"，检查模式改成拿这一下的时刻去对谱面第几个音
   （`expectedAtMs(i) = (t_i − t_0) × 谱面速度 ÷ 用户速度`）；
2. 中间被跳过的音**直接记漏**（检查模式不等你）；
3. 每个音算偏差 `devMs = 起音时刻 − 该响的时刻`，容许偏差
   `clamp(0.25 × 时值, 60ms, 250ms)`，**第一个音给 2 倍**；
4. 超出容许 → 记"抢拍/拖拍"（单独计数）；整段总结多一行：
   `节奏：偏差中位 +248ms／最大 248ms，抢拍 0 处、拖拍 1 处（容许 ±150ms）`。

**为什么不合成一个"对/错"**：用户被标红时得能看出是音错了还是拍错了。
这三个参数（0.25 / 60 / 250）是起点值，不是行业公式 —— 没有那种公式，
公开实现（web-score-following 的 OTW、Raphael 粒子滤波）用的都是跟踪器。要调就调这三处。

合成回归新增场景 5/6（`test/test-follow-page.mjs`）：
· 两个音都踩在点上 → 对 2、抢拍 0、拖拍 0；
· 第二个音拖 250ms → 音准仍判对，节奏行报"拖拍 1 处"。

### 9.2 节拍器
产品页加了「节拍器」开关（默认关，提示戴耳机）。

**这一项我一开始做重复了**：调试图那条链路（`frontend/js/metronome.js`）早就有一个节拍器，
而且是**业界标准做法** —— 每 25ms 往未来看 200ms、把拍点预约到 Web Audio 的时钟上
（Chris Wilson《A Tale of Two Clocks》；Tone.js 的 Transport 也是这套）。
我在产品页又手写了一份（120ms/500ms 窗口），还漏掉了它专门写下来的那条教训：

> 起振故意做软（20ms），不是"啪"的一下。拨弦的起振在 5ms 以内，而节拍器的响声会被麦克风
> 收进去；起振软了，起音检测就不容易把它当成一次拨弦。

已经**合并成一份**：抽 `frontend/js/metro-core.js`（不认界面、不认状态），
产品页和调试图都用它。顺手补了两处原来没有的：

- `<=` 而不是 `<`：原来第一拍要等下一次定时器醒（慢 25ms）；
- 落后超过一拍（后台标签页被降频、主线程卡住）时**不要一口气补响一串**，
  直接对齐到现在往后 —— 节拍器要的是当前这一拍。

新增 `test/metro.mjs`（原来节拍器一点测试都没有）：拍点不累积抖动、卡住不补响一串、
**起振必须 ≥15ms**、停止后不再排拍。

---

## 10. 光标层：117 vs 118 的真正来源（已修，附证据）

### 10.1 结构性问题：光标和判定不是同一份清单

跟弹页一直是**两份清单**：

- 判定清单 = 时间轴 `frontend/data/hey_jude.json`（118 个音，一次拨弦一个）；
- 光标位置 = alphaTab 解析出来的谱面拍点（`collectScoreSlots`）。

用户导出记录里写着 `beatsFromScore: 117` / `notesFromTimeline: 118` —— **差一个**。
两份差一个，"第 i 格"就是不同的音 → 照着光标弹必然判错，而且错位点之后全体错开一位。

### 10.2 差一个的根因：延音过滤条件把**起点**也丢了

浏览器里真正跑的那份构建（`frontend/vendor/alphaTab.min.js` 原文）：

```js
tieOrigin = null; tieDestination = null; isTieDestination = false;
get isTieOrigin() { return null !== this.tieDestination }
```

即 `tieDestination` 挂在**延音起点**身上（它指向"接续到哪一个音"），
"我接在别人后面"这个意思只有 `isTieDestination` 有。

而上一版的判据是：

```js
const isTieDest = (n) => !!(n.isTieDestination || (n.tieDestination && n.tieDestination !== n));
```

**"或"上了起点** → 延音的起点和接续**两拍一起丢**。

用 PyGuitarPro 直接数这份谱（新增 `test/probe-gp3-beats.py`）：

```
轨 0「Voice」：119 个有音符的拍，其中 1 个是延音接续  →  要弹 118 个音
第 1 个音：小节1 拍1 tick960  2弦1品(C4)   ← 用户说的"开头那个 2 弦 1 品"
```

119 − 2 = **117** ✓ 和导出里的数字对上。修法：`isTieDest = (n) => !!(n && n.isTieDestination)`。

### 10.3 改成"一一对应"，并且每次加载都自己验一遍

`buildTickMap` 里的映射换成纯函数 `mapSequenceToSlots`（能离线测）：

- 两份数量一样 → **逐条验音高 + 品**（跟弦号怎么编号无关），全对才用一一对应，
  页面上写「对齐 ✓ 谱面118=判定118」；
- 对不上 → 标 ⚠ 写明差多少（`mismatched` / `matchedFail`），退到"按品+音高只往后找"的单调配法，
  **绝不悄悄错开**。

不再依赖 `beat.absoluteStart`（手机上它常常是 undefined，以前按时间配会全体塌到第一拍
→ "光标停在第一个不动"）。判定那条路本来也不看时刻：练习模式就是"你不弹我不走"。

对齐结论写在「导出记录」旁边的 `#align`（手机上不用开控制台），导出记录里也有。

### 10.4 顺手补的两处

- **开始就把光标画在第一个音上**：`startMic` 里 `buildTickMap` 之后调一次
  `highlightCurrent()`（以前要判完第一个音才出现，看着就是"没从第一个音开始"）；
- **点击谱面换练习位置**重新打开，但**跟弹进行中点它不再改起点**（手指划到谱面碰一下
  就会让剩下的音全错位，先要求点「停止」）；默认仍然**永远从第一个音开始**。

### 10.5 这一层的验证

`test/test-follow-page.mjs`：

- §13 复现 117 的成因（起点带 `tieDestination`、接续带 `isTieDestination` → 只丢 1 拍）；
- §11 映射：逐条对得上 → 一一对应；少一格 / 内容对不上 → 必须标 ⚠；
- §13b 整条链路走一遍：假谱面（含延音接续）+ 时间轴 → 光标表必须是
  `[第1拍, 第2拍, 第4拍]`（延音那拍不占格）。

`test/test-follow-real.mjs`（用户 30 秒真机录音、全部弹对）在改动前后都是
**对 29 / 错 6**，说明这一轮没动判定层。音高测量那条（弹错判对、1弦1品轻音）
仍旧是**测量层**的事，正解是"起音前后短窗的差分谱"，要在真机录音上离线量过再上。

## 11. 差分谱判定的第一次离线实测：**没通过，不要上线**

方向是对的（"把上个音减掉，只剩这一下新拨的"），实现是：起音那一刻
`buf`（341ms）切两半 → 各做 8192 点谱 → `diffMags(post, pre)` → 在 ±250 音分内
用 `estimateF0Near` 量，`|cents| ≤ 75` 判过。

两个方向的数（都在这台机器上跑出来的）：

| 判据 | 合成（test-follow-page 弹错音矩阵） | **真机录音**（30 秒、全弹对） |
|---|---|---|
| 老：起音快照 ±80 | 8 种错法**全部判对**（已知缺口） | **对 29 / 错 6** |
| 新：差分谱 ±250 | 8 种错法**全部判错**（缺口补上） | **对 10 / 错 25** ❌ |

结论：**合成只有一根弦在响，看不出这个坑**（这已经是第三次被合成信号骗）。
两个 170ms 半窗做幅度相减，在真机（衰减中的音、相位/包络都不一致）上减完的残余
把谐波结构打散了 —— 逐条看错音清单，读数是"约 B3 要 C4""约 G#3 要 A3"这种差一点点的假读数。

代码现状：判定**仍旧用老快照**（`JUDGE_DIFF` 默认 false），差分谱只**量、只记**
（导出记录里 `lit` / `dCents` / `dHz` 三列）。开关留着，离线对照用：
`window.__judgeDiff = 1` 或 `VC_JUDGE_DIFF=1 node test/test-follow-real.mjs …`。

下一步要试的（都不许在手机上试）：

1. 相减改成"**只取起音后 40~60ms 的短窗**"（用多谐波联合投票补回频率分辨率），
   或者对两半先做**包络归一化**再减（`analysis.js` 的 `removeDecayBias` 就是干这个的）；
2. 同一根弦重复音那种自相消的情况要单独处理（减完基频会掉进噪声里）；
3. 评价指标就用这两份：真机录音 30 秒（要 ≥ 29/35 且不能把对的判错）+ 合成错音矩阵（要 8/8 判错）。

---

> **收工了。** 明天接着做，先看 `记忆同步-2026-09-21-收工状态.md`
> （一页纸：当前版本号、手机上要验的三件事、下一轮按优先级排的清单、常跑的命令）。
