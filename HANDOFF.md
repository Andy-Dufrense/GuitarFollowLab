# 交接文档 · 吉他跟弹识别

> 写于 2026-09-18。下次接着做，先读这一份。
>
> ⚠ 这份是 09-18 写的、讲调试图那条链路，**有些内容已经过时**。
> 跟弹页（产品页）的最新状态看 `记忆同步-2026-09-21-收工状态.md`（2026-09-21 晚），
> 过程细节看 `记忆同步-2026-09-21-跟弹.md`（第 10、11 节是光标层和差分谱那两轮）。

---

## 一、这个项目要做什么

**场景**：给用户一份作业（谱子），用户拿着谱子练琴，系统判断他弹对没有。

**必须同时支持两种模式**：

| | 慢练（过关式） | 原速（跟弹式） |
|---|---|---|
| 节奏 | 用户自己控制 | 按谱面时间轴走 |
| 错了 | 停下来原地重弹 | 不停，只标记对错 |
| 流畅度 | 要求低 | 要求高 |

**当前只做了慢练那一套。原速跟弹还没动。**

---

## 二、现在能跑成什么样

### 已经能做的

| 项目 | 实测结果 |
|---|---|
| 六根空弦 + 按弦音的音高检测 | ±1~3 音分 |
| 音色无关性（五种完全不同的音色） | 全部整段通过 |
| T3231323 琶音 @ 60/80/100/120 BPM | 整段通过 |
| 扫弦判对 / 判错 | 通过 |
| 变调夹 0~7 品 | 通过 |
| 节拍器 + 逐音偏差统计 | 通过 |
| 手机访问（https + 证书） | 通过 |
| **手机麦克风频响**（低频高通，六弦 82Hz 被压掉） | 通过 —— 六根空弦间隔 450ms / 700ms 都整段过 |
| **和弦余响里弹低音弦**（先扫 C 再弹六弦 E2） | 通过 —— 不再判成高一个八度（2026-09-20 修） |
| **完全不消音 + 手机麦克风**（T3231323 @ 60/80/100 BPM） | 通过 |

### 已知做不到的

| 问题 | 现状 |
|---|---|
| T3231323 @ 140/160 BPM | 过不去（八分音符短于 214ms） |
| 低音弦 + 和弦余响 → 判高一个八度 | **已修**（双向失配打分 + 音域过滤，见第五节） |
| 判错后 300ms 内弹的音听不见 | **已修**（判错立刻恢复监听，原来被一个 setTimeout 挡着） |
| 极端延续（衰减慢 5 倍）+ 快弹，不带手机频响的合成信号 | 60BPM 过，80/100 不过（test-live 第 13 组；带手机频响时反而能过） |
| 原速跟弹模式 | 完全没做 |
| 开场标定（逐弦测音色） | 只设计了，没实现 |
| 真机录音验证 | **仍然只有合成信号**（`.gp/` 里已有一个真实 .gp3，但那是谱面素材，不是录音） |

---

## 三、核心设计原则（改代码前先看这个）

### 1. 不预设音色

用户手上的琴千差万别，**不能拿一把琴的参数去调**。系统只能用跨吉他不变的物理事实：

- 谐波出现在基频的整数倍位置上（琴弦刚性只让它略微拉伸）
- 衰减中的余响只会往下走，新拨的一下会往上跳

**所有判定必须是相对比较，不能有绝对阈值。**

### 2. 有谱信息要用足

既然知道该弹什么，就**不必回答"这是什么音"**（开集问题），只需要回答
**"新出现的这坨能量最像哪个候选音"**（闭集问题）。

这条是项目里最重要的一次转向。之前是"YIN 先测绝对值再比对"，
那个绝对值会被音色、麦克风频响、琴弦刚性、房间一起带偏，
所以一路都在打补丁（八度特判、刚性修正、差分兜底）——
**补丁全是在错误前提上修补。**

### 3. 显示和判定必须用同一套方法

否则会出现"显示说 E2、判定说 E3"这种自相矛盾，用户看到的就是"耍人"。

---

## 四、当前检测算法（重点）

### 起音：两条判据满足其一

```
① 电平 > 48ms 前的 1.6 倍                       ← 干净环境最直接
② 频谱通量 > 0.16 且 电平 > 48ms 前的 1.15 倍   ← 对付"延续不消音"
且 电平 > 门限（噪声地板 × 4）
```

为什么要两条：你让音一直延续时，几根弦的余响叠起来的总电平**可能比新拨的那一下还响**，
光看 ① 永远触发不了——这就是"消音再弹很丝滑、延续着弹很迟钝"的原因。
而余响只会衰减、不会凭空产生新成分，所以 ② 对延音免疫。

### 判定单音：候选音谐波比较（在 analysis.js）

```
① 逐频点本底 updateBackground：上升 8%/次，下降 25%/次
   → 把"已经在那儿的能量"（余响）吸进本底
② novelSpectrum = max(0, 当前频谱 − 本底)      ← 新出现的能量
③ 从 novel 里取**最强的 12 个局部极大值**当"观测到的峰"
   （不能按"超过某个幅度"来挑：音一衰减，novel 里就全是噪声小峰，一帧能挑出 400 多个）
④ 对候选音算**双向失配**（Two-Way Mismatch，单位音分，越小越像）：
   P2O：我预测该有的每个谐波，观测里有没有？缺了记一笔（低次谐波权重 1/√k 更大）
   O2P：观测里每个峰，我能不能解释？解释不了记一笔（按峰的能量加权）
   候选 = 目标音 + [0, -1, +1, -2, +2, -12, +12, -5, +5] 半音，
          再按**乐器音域**过滤（最低空弦 ~ 最高 24 品；E1 这种弹不出来的直接不要）
   琴弦刚性系数不知道 → 试 4 个值 [0, 0.0002, 0.0005, 0.001]，取失配最小的
⑤ 分数 = 1 / 失配；目标音领先次优 1.08 倍以上 → 判过
⑥ 音准扫描：把目标往 ±¼、±½ 个半音挪一下，看是不是明显更像
   更像 → "弦没调准，先调一下"（既不放过去，也不说弹错了）
```

**为什么这套不吃音色**：判据全是**相对量** —— 候选之间的比值、预测与观测差多少音分。
换把琴、换个手机只会改绝对幅度，改不了"峰在哪里、离预测位置多少音分"。

**八度不用特判**：弹 A2 时，A3 这个候选只能命中它的一半谐波
（110、330 那些奇数谐波它没有），分数天然低一截。

**为什么必须双向**：只"看该有能量的地方有没有能量"，谐波系列互相包含的音会互相蹭分 ——
低八度候选把真音的每个谐波都当成自己的偶数次谐波，高八度候选同理。
"先扫 C 和弦、再弹六弦 E2 判成 E3"就是这么来的（C 和弦里的 E3/E4 正好是 E2 的 2、4 次谐波）。
双向失配把"该有的缺了"和"多出来的解释不了"都算进账，这类平局就消失了。

**音准扫描为什么不违反"别退回开集"**：它问的不是"这个音有多高"，而是
"比谱上那个音偏了多少" —— 还是拿已知答案去比，得到的仍然是相对量。

### 判定和弦：Chroma 音级指纹

和以前一样（见 dsp.js 的 matchChordIn）。先把观测到的音级按变调夹转回来，
再在一组原位和弦里挑余弦相似度最高的。

### YIN 还在，但只做两件事

- 滑音轨迹显示（要看音高连续滑动）
- 逐音偏差（音分）的读数

**判定已经不看 YIN 了。**

---

## 五、踩过的坑（都别重踩）

### 起音检测类

1. **不能要求"先彻底安静下来才能再次触发"**。只要上一个音还在响、或房间底噪偏高，
   后面弹的音就永远触发不了。现象是"听到一直在变，但判定根本不跑"。
2. **基准不能用"最近几百毫秒的最大值"**。连弹同一个音会卡死——第二个音的峰值并不比第一个高。
3. **基准不能用"平均值"**。几根弦之间的拍频会让余响忽高忽低，误触发。
4. **48ms 基准窗是调出来的**。缩到 32ms 会让判定点提前，120BPM 反而挂掉。
5. **频谱通量不能单独用**，必须配合一点电平抬升，否则被余响的频谱抖动骗到。
6. **通量窗 512 点太粗**（48k 下只有 10.7ms，bin 宽 94Hz，低音弦分辨不出来），要用 2048 点。
7. **逐频点本底上升速率不能太慢**。原来是 2%/次（时间常数约 3 秒），
   上一个和弦响 800ms 才吸收 23%，剩下七成多的余响混进"新出现的能量"里。
   改成 8%/次（约 500ms）——隔 800ms 能吸掉六成多，
   而新音进来后 130ms 的判定窗口里只被吸掉一成半，不影响判定。

### 音高检测类

8. **不要在滞后域里猜八度**。加过"τ/2 处也不差就把音高翻倍"的修补，
   结果被强二次谐波骗到：**六弦 E2→E3、四弦 D3→D4、一弦 E4→E5**。
9. **不能用"是不是一个峰"区分八度**。实测 A2 的二次谐波在 220Hz 处
   是个比基频还尖锐的峰，突出度 4275 —— 谐波本身就是峰。
10. **不要在 YIN 前面加低通**。压高频确实能减轻亮音色的偏高，
    但在"上一个音还在响"的混合信号里会把相位关系搅乱——实测把 A3 判成 D2（差 3 个半音）。
11. **琴弦刚性会让读数偏高**。第 k 次谐波实际是 `k·f0·√(1+B·k²)`，
    真实钢弦量级（B≈0.0008）下亮音色能偏高 **+58 音分**——越过 50 音分就取整成隔壁半音。
    解法：YIN 给粗估计 → 去频谱找基频峰的位置修回来（±1 半音内找，找太远会跳到谐波上）。
12. **fftSize 要传"实际做 FFT 的点数"**，不是缓冲区长度。dec 长度可能不是 2 的幂，
    `spectrumOf` 会往下取，传错的 bin 宽就全错了。

### 打分函数 / 判定类

12b. **不能只问"该有能量的地方有没有能量"。** 这是 2026-09-20 修掉的那个八度 bug 的根：
    谐波系列互相包含（E3 的谐波全是 E2 的偶数次谐波），只算"命中"就会互相蹭分。
    必须同时问"该有的缺了没有"（P2O）和"多出来的解释不了没有"（O2P）—— 双向失配。
12c. **观测峰要取"最强的 N 个"，不能按幅度门限挑。** 音一衰减，novel 里剩下的基本是噪声起伏，
    用"最大值的 8%"当门限，一帧能挑出 400 多个噪声峰，失配的账全被噪声占满，判定变随机。
    （`probe-octave.mjs` 打印的峰表就能看到这个。）
12d. **失配的封顶值不能太小。** 原来截在 40 音分，"偏 48 音分"和"偏 246 音分"就分不出来了 ——
    实测偏低近半个音会被判成"听到 F#2"（差两个半音）。放到 150 音分才对。
12e. **判错之后不要用 setTimeout 挡输入。** 原来是 `setTimeout(300ms)` 才把相位放回 waiting，
    实机上就是"弹错了以后 300ms 内弹的音全都听不见"，密集连弹必然踩中；
    而且 setTimeout 和主循环用的不是同一个时钟，测试里根本复现不出来。现在判错立刻恢复监听。
12f. **诊断脚本必须和页面的取样方式一致**，否则量出来的是假象。这个坑踩了两次：
    ① 窗口方向 —— 页面给的是"最近 341ms"，探针一开始写成了"往后 341ms"；
    ② 判定时刻 —— 页面判定时是**现算**一帧频谱，探针一开始用的是上一帧留下的。
    另外循环节奏也要对上（主循环约 60ms 一次，用 16ms 跑会让本底吸收快好几倍）。
12g. **加过滤条件时要给空结果兜底。** 给候选加"乐器音域"过滤之后，
    和弦事件（没有 targetMidi）会把所有候选都过滤掉，`find(...)` 返回 undefined，
    原来"算出来全是 0 分"的静默无效变成了崩溃。过滤和查找都要假设"可能一个都不剩"。

### 流程 / 状态机类

13. **补测逻辑必须放在 `S.phase = 'cooldown'` 之前**。放后面的话，补测返回时状态已经变成
    cooldown，主循环的 `phase === 'settling'` 判断不成立，**再也不会调进来**——现象是"卡在正在听清"。
14. **主循环一定要包 try/catch**。这个项目靠它抓到过至少三次错（变量名漏改、模块引用错），
    否则表现就是"页面静默卡死"。
15. **"听到"读数要在起音窗口内更新、之后保持不变**。早期用通用检测每 60ms 刷新，
    结果拨弦瞬间对、几十毫秒后被上一根弦的余响顶掉——用户看到的就是
    "音名对了一下就被延音盖过去"。
16. **`renderStep` 里不要清空"听到"**。每换一步清成"—"，用户会觉得"刚弹对了就没了"。

### 工程 / 环境类

17. **`.bat` 和 `.ps1` 里只能写 ASCII**。cmd 用 GBK 解析，UTF-8 中文会乱码甚至把命令行拆断；
    PowerShell 5.1 读 .ps1 也一样（除非带 BOM）。
18. **不要用 PowerShell 做"读-改-写"去改 JS 文件**。`Get-Content -Raw` + `Set-Content -Encoding UTF8`
    会加 BOM 并把中文注释弄乱，**两次把文件写坏过**。改文件用 apply_patch。
19. **这台机器默认浏览器是 IE**（注册表里只注册了 IE；Edge 装成 `EdgeCore\<版本>\` 形态，
    标准启动器目录是空的）。start.bat 自己找 msedge.exe/chrome.exe，
    绝不能用 `start "" "http://..."`。
20. **这台电脑没有任何麦克风设备**（只有 Headphones 和 S/PDIF 两个输出）。
21. **手机走局域网 http 拿不到麦克风**（不是安全上下文）。要么装证书走 https，
    要么安卓在 `chrome://flags/#unsafely-treat-insecure-origin-as-secure` 里把这个地址加进白名单。
22. **拆模块之后测试会互相污染**：状态搬到 `state.js` 后，测试用 `?case=X` 只能让入口重新加载，
    `state.js` 是共享的。解法是 `resetApp()`；但 `CFG`（用户设置，比如变调夹）它**不清**——
    真实使用中换过变调夹，复位进度不该把它清掉。**测试要自己记得归零。**

---

## 六、目录结构

```
start.bat                    一键启动（默认不开浏览器；加 -o 才开）
package.json                 npm start = node backend/server.js
README.md                    项目说明 + 所有技术决策的记录
HANDOFF.md                   本文件
.gp/                         真实 Guitar Pro 文件（跟弹时间轴素材，待接入）

frontend/
  index.html                 页面
  live.css                   样式
  js/main.js                 入口：装配 + 主循环 + 按钮接线
  js/config.js               全部可调参数和常量
  js/state.js                共享状态 + 练习进度
  js/audio.js                麦克风采集、音频时钟、权限查询
  js/analysis.js             信号分析：本底 / 双向失配 / 音准扫描 / YIN / 通量
  js/judge.js                判定状态机：单音、和弦、变调夹、反馈文案
  js/ui.js                   全部 DOM 渲染 + 麦克风诊断面板
  js/metronome.js            节拍器（用音频时钟排拍子）
  js/dsp.js                  DSP 内核：YIN / FFT / Chroma / 和弦比对
  js/exercises.js            五个模式的内容
  js/data.js                 音高表和和弦指法库

backend/
  server.js                  静态服务器：http 1209 + https 1210（静态根指向 ../frontend）
  make-cert.bat / .ps1       生成手机用的证书
  certs/                     证书（root 的 .cer 给手机装，leaf 的 .pfx 服务器用，密码 guitarlab）

test/
  test-dsp.mjs               DSP 内核（合成信号，不需要麦克风）
  test-live.mjs              实时链路端到端（假麦克风）
  test-detect.mjs            **检测能力专项**：手机频响 / 余响里的低音弦 / 密集连弹 / 不消音
  test-mic-fail.mjs          麦克风开不了的三种情况，各该报什么
  test-bughunt.mjs           挖 bug：变调夹/技巧/转换/静音/噪声/练完/音色无关性
  probe-flux.mjs             诊断：起音判据的读数（电平抬升 / 频谱通量）
  probe-octave.mjs           诊断：和弦余响里的八度问题出在哪
  probe-flat.mjs             诊断：音准偏离与失配的关系
```

依赖方向（单向，无环）：

```
main.js → analysis.js → dsp.js, config.js
        → judge.js    → state, analysis, ui, dsp, metronome
        → metronome.js→ state, ui, audio
        → ui.js       → config, state, audio
        → audio.js    → config
        → state.js    → config, exercises
```

---

## 七、怎么跑、怎么测

```bat
cd /d E:\GuitarFollowLab
node server.js
```

浏览器开 `http://localhost:1209`（手机用 `https://192.168.0.81:1210`）。
也可以双击 `start.bat`。**不要用 IE。**

### 每次改完必跑

```
node test/test-dsp.mjs        # DSP 内核
node test/test-live.mjs       # 实时链路（含 60~160BPM 的琶音表、音准校准用例）
node test/test-detect.mjs     # 检测能力（手机频响 / 余响 / 密集连弹 / 不消音）
node test/test-mic-fail.mjs   # 麦克风开不了的各种情况
node test/test-bughunt.mjs    # 变调夹/技巧/转换/静音/噪声/练完/音色无关性
```

**五个都必须退出码 0。** 其中 bughunt 的 G 组（音色无关性）是这套东西的验收标准，
改任何检测相关代码之后必须看它是不是还全过。

改打分函数/阈值时，先跑这三个诊断脚本把数字打出来，别凭感觉调：

```
node test/probe-flux.mjs
node test/probe-octave.mjs
node test/probe-flat.mjs
```

### 改完也在真浏览器里过一眼

```
& 'E:\Lib\ms-playwright\chromium-1234\chrome-win64\chrome.exe' `
  --headless=new --disable-gpu --no-sandbox --virtual-time-budget=5000 `
  --dump-dom http://localhost:1209
```

看 `id="bootfail"` 是不是空的（空 = 脚本正常启动）。

> ⚠️ 2026-09-20 实测：**这台机器上跑不了**。Chromium 报
> `Sandbox cannot access executable E:\Lib\ms-playwright\...\chrome.exe: 拒绝访问`，
> 加 `--no-sandbox` 也没用（它自己的沙箱检查在先）。这是文件权限问题，不是代码问题。
> 替代做法：`test-live / test-detect / test-mic-fail` 都是加载**真实的 main.js** 跑主循环的，
> 语法或装配错误一定会在那儿炸出来；再加上 `node --check frontend/js/*.js` 做静态检查。

---

## 八、下次从哪继续

### 优先级 1：真机验证（最要紧）

现在所有验证都是**合成信号**。最缺的是真实录音——
用手机录一段自己弹 T3231323 的音频（wav/m4a 都行），丢进项目文件夹，
直接分析真实信号，比继续调合成参数有用得多。

重点确认两件事：

- **二弦（B3）过不过得去**（上一版卡在这里，改成候选比较之后没在真机验过）
- **先扫和弦再弹低音弦，会不会判高一个八度**（合成测试里确实会，真机频率未知）

### 优先级 2：开场标定（可选）

既然不能预设音色，就现场测。用户开始前每根空弦弹一下，拿到：

- 这把琴、这个手机下的逐弦基频偏差 → 调音基准
- 逐弦的谐波能量分布 → **这才是模板，是从他琴上测出来的**
- 这个房间的噪声底 → 门限基准

大概十秒钟的操作。不做也能跑（现在的算法已经不吃音色），做了更准。

### 优先级 3：原速跟弹模式

> 前置条件：先把真机录音这条过了。用户明确说过 —— 检测基础（听得到、听得对）没打好之前，
> 不会给真实 .gp 素材做跟弹测试。`.gp/the-beatles-hey_jude.gp3`（2026-09-20 放进来的）
> 可以先拿来验证"谱面 → 时间轴"这一段：解析出音符/时值/速度/反复，先不接音频。
> 注意 `E:\Python` 里**没有** PyGuitarPro（`No module named 'guitarpro'`），
> 要用得先装到 `E:\VirtuCoach-Lib`，或者直接用 alphaTab 在浏览器里解析。

这是"流畅"的关键，也是用户明确要的两种模式之一。

架构已经分开了（analysis.js 检测 / judge.js 策略），所以是在 judge.js
里补第二条策略，不用重做：

- 维护状态 = **谱面位置 + 速度**，每帧都用当前证据更新
- 用 HMM 或粒子滤波（学术界的标准做法，叫 score following）
- 界面显示"我现在认为你弹到哪了"，而不是"这个音过了没有"
- 用户不用等确认，快弹慢弹都能跟

### 优先级 4：速度上限

现在 120~140 BPM（八分音符 250~214ms）之间是极限。根本原因是
**把整个信号当成一个音在测**，而实际上同时有好几个音在响。

再往上要么按弦分别跟踪（每根弦一个频率范围，判断"这根弦的基频在不在"），
要么上真正的复音检测（NMF / 多 F0 / 神经网络）。这是架构级改动。

### 优先级 5：修"低音弦 + 和弦余响判高八度"

合成测试里的复现方式：先扫 C 和弦，0.8 秒后弹六弦 E2 → 判成 E3。

已知原因：C 和弦本身含 E3、E4，正好是 E2 的 2 次、4 次谐波位置。
所以"新出现的能量"里混着 C 和弦残留的 E3/E4 时，
E3 这个候选（它的谐波正好是 E2 的偶数次谐波）能拿到不低的分。

可能的方向：给候选打分时对"只命中偶数次谐波"的情况加权惩罚，
或者要求候选自身的基频位置上确实有新能量。

---

## 九、几条要记住的判断

- **"知道标准答案"是最大的优势**——别退回开集识别。任何时候想加一个
  "先测音高再比对"的逻辑，先想想能不能改成"直接验证期望音的谐波在不在"。
- **任何绝对阈值都是可疑的**。要么改成相对比值，要么改成从当前信号里估出来。
- **测试要能复现问题**。这个项目每一个真 bug 最后都是靠"先写一个能复现的测试"
  才定位的（一弦偏高、B=0.0008、补测卡死、二弦……）。
- **合成验证不能替代真机**。合成信号和真实吉他最大的差别在四处：
  非谐性系数、基频相对强度、余响长度、琴体共鸣带来的频谱包络。

---

## 十、更早的历史（不用细看，需要时再翻）

README.md 里按时间顺序记了每一个技术决策和它的理由，包括：

- 为什么低音弦必须用 341ms 的长窗（12kHz 下 1024 点 FFT 的 bin 宽 11.7Hz，
  而 A2=110Hz 处一个半音才 6.5Hz）
- 为什么 Chroma 不能按频率 bin 累加（每个音级都会分到能量，12 个全亮）
- 为什么和弦比用余弦而不是集合重合（吉他和弦的泛音会让个别音级虚高）
- 为什么实时页不判转位（C 和 C/G 的音级集合完全一样，只有低音不同）
- 为什么宽容要落在"过渡区间"而不是整段音，而且要拿到手势的正面证据
