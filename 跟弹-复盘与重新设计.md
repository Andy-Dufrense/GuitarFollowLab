# 跟弹：复盘与重新设计（2026-09-20）

> 只讲两件事：现在错在哪、应该怎么做。
> 来源：我们自己的实测 + 开源实现 + 近两年论文（出处见文末）。

## 一、一句话结论

**我把"跟着用户走"和"指挥用户弹"混成了一个东西。**
成熟产品的谱面光标是**指令**（告诉你下一个弹什么），不是**追踪器**（显示你弹到哪了）。
我做成后者，于是所有症状都长出来了。

| 你看到的现象 | 根因 |
|---|---|
| 光标在匹配"我弹到哪了"，不是提示"我该弹什么" | 我把光标交给声音事件/时间驱动，光标跟着过去式走 |
| 延音古怪、同一个音要弹两次 | 我把"起音事件"当唯一推进依据；延音没有新起音，于是要么等窗过期、要么把延音当新音 |
| 弹对却被判错 | 判定在"刚弹的那一下"上做比较，候选里还留着 ±5、±12 这些能"抢票"的音 |
| 环境音被当音符 | 事件驱动模型里"响一声"就有机会变成"下一个音" |
| 不像练琴、生硬 | 缺练习闭环：准备→按谱提示→逐音反馈→整段结果→重来，我只做了中间一段 |

## 二、正确的心智模型

谱面 = 一串音符事件，每个事件带三件信息：**起音时刻 t、时值 dur、音高/弦品**。

每个音的循环：

1. 光标停在这个音上（指令：弹这个）
2. 等我弹（默认）或按拍听（跟节拍模式）
3. 在时间窗内验证"谱面要的音出现了没有"
4. 结果只有三种：对 / 错 / 漏，三种都往下走（错不停）
5. 时值之内是延音：不期待新起音、不判错、也不推进

**判定不是"识别这是什么音"，而是"谱面要的那个音，此刻出现了没有"。**
这条结论我们很早就对了，但实现里一直没贯彻——候选集里始终留着抢票的选项。

## 三、外部对照

| 项目/论文 | 许可 | 做法 | 该学什么 |
|---|---|---|---|
| Matchmaker（ISMIR 2025，github.com/pymatchmaker/matchmaker） | Apache-2.0 | 实时谱面跟随参考实现：谱面表示 × 在线对齐，配标准数据集与指标；明确用途含自动伴奏、**自动翻谱** | 对齐是独立一层，与听音分开；必须有评测集与指标 |
| web-score-following（WAC 2024，github.com/matthewcaren/web-score-following） | MIT | 浏览器里 CENS + 在线时间规整（OTW）维持谱面位置 | 在线对齐可以很轻，浏览器也能跑 |
| STRAdi（ISMIR 2026） | Apache-2.0 | 小提琴因果流式转写 + 谱面跟随 | 若上模型必须"因果" |
| alphaTab（github.com/CoderLine/alphaTab） | MPL-2.0 | 谱面渲染 + 播放 + 光标 + 变速 | 光标/翻谱属渲染层，应由"当前该弹哪个音"驱动 |
| Yousician 论文（arXiv 2510.05756） | — | 内容侧：分轨 + MERT 转写节奏型 | 实时判定侧不公开，别指望抄 |

## 四、重新设计（分层）

1. 谱面层：.gp → 音符事件表 [t, dur, midi, 弦, 品]（已有 gp_timeline.py）
2. 渲染层：alphaTab 渲染 + 我们的光标（= 当前该弹的音）+ 当前行滚到第一行 + 点谱面定位
3. 判定层（重写）：每个音一个时间窗；窗内找起音；候选只用目标与邻近（±1/±2/八度）；延音段不判；窗内无起音 = 漏
4. 对齐层（仅跟节拍模式）：在线时间规整，输出"谱面位置 + 速度"，不必每帧重算
5. 结果层：对/错/漏三个数 + 逐音清单 + 整段汇总
6. 数据层：会话记录导出 JSON，用于离线标定

## 五、立即要修的 8 件事

1. 光标只标六线谱那一行（现在连五线谱一起框，方向反了）
2. 光标 = 指令：只在判完上一个音时前进，任何声音/任何时间都不许它自己走
3. 延音彻底不判：时值内不做判定，也不需要第二次起音
4. 候选集收窄：只留目标与邻近（±1/±2/八度）
5. 点谱面可定位：点某个音就从那开始练
6. 练琴闭环：开始→四拍→按谱提示→逐音反馈→整段结果→重来
7. 模式显式化：等我弹（默认）/ 跟节拍（需在线对齐，后做）
8. 评测集：用"跟着试听弹一遍干净的"当标准答案；指标 = 对/错/漏 + 逐音时间偏差

## 六、出处

- Matchmaker：arXiv:2510.10087（ISMIR 2025）；github.com/pymatchmaker/matchmaker（Apache-2.0）
- web-score-following：github.com/matthewcaren/web-score-following（MIT，WAC 2024）
- STRAdi：github.com/seingreen/STRAdi（Apache-2.0，ISMIR 2026）
- alphaTab：github.com/CoderLine/alphaTab（MPL-2.0）
- Yousician：arXiv:2510.05756
