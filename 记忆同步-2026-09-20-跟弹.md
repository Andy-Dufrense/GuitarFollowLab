# 记忆同步 · 2026-09-20 · 跟弹（吉他作业评估）

> 明天接着做，先读这一份。上一份设计复盘在 `跟弹-复盘与重新设计.md`。

## 0. 一句话现状

产品页已经能**渲染真实谱面（.gp3）+ 跟着用户弹逐音判定 + 在谱面上标对错**。
判定质量：用用户那段 30 秒 Hey Jude 录音实测 **对 28 ／ 错 3 ／ 测不准 9**，连续相同音整组通过。

## 1. 怎么跑

```
cd /d E:\GuitarFollowLab
start.bat                      :: 产品页 http://localhost:1209/
                               :: 手机 https://<电脑IP>:1210/（手机必须 https）
```

- 产品页 `frontend/index.html`：谱面 + 试听 + 跟弹 + 谱面标对错 + 结束汇总
- 调试页 `frontend/debug/`：练习台、格子版跟弹、和弦版
- 注意：端口 1209/1210 常被**旧进程**占着（改了代码看不到效果多半是这个）。
  查：`Get-NetTCPConnection -LocalPort 1209,1210 -State Listen` 找到 PID 再停掉。

## 2. 已经定下的产品口径（不要再摇摆）

1. **不做节奏**：不判抢拍拖拍，只回答"这一个音是不是谱面要的答案"。
2. **单音谱逐音判**；**和弦谱判有没有和弦外音**（`chordOutsiders`，不逐音）；
   **扫弦只判和弦**；**技巧判过程**（滑音已做：必须有从起点滑到落点的轨迹）。
3. **错了不停**：标红继续；**正确率 ≥90% = 这份作业算完成**。
4. 用户是**随手弹**、琴可能**没调准** → 必须容错 + 提示调音（按弦给）。

## 3. 当前判定链路（代码位置）

```
麦克风 → 起音判定（frontend/js/follow-score.js 的 micTick）
   三条线索：
     ① 电平比 48ms 前抬升 1.5×
     ② 全频通量 > 0.18 且电平抬升 1.15×
     ③ 重复音专用：高频(2kHz+)通量 > 0.10 且电平抬升 1.15×（两条必须同时成立）
   最小间隔按谱面间距自适应（快音 ×0.55，重复音 ×0.35，兜底 55~160ms）
 → 峰值快照：起音那一刻取 8192 点（170ms，bin 5.9Hz），再做抬头过滤（只留上升的频点）
 → 音高：analysis.js 的 estimateF0Near
        （期望音 ±80 音分内、5 音分步长扫，每谐波 ±15 音分容差，按谐波个数归一化）
 → 音准基线校正：全局中位数 + 按弦中位数（≥5 样本，向全局收缩一半）
 → 判定：只有"邻音能量高出 30% 以上"才判错，其余判过（知道标准答案 → 无明确反证就算对）
 → 结果：对 / 错 / 测不准，谱面标绿 / 红 / 灰
```

## 4. 实测数据（用户 30 秒录音 sound_data/f32/hey_jude.f32）

| 版本 | 对 | 错 | 测不准 |
|---|---|---|---|
| 最早（开集候选比较） | 4~5 | 23~36 | — |
| 收紧容差 + 测不准分类 | 17 | 3 | 9 |
| 删掉"延音期"（关键） | 31 | 2 | 9 |
| 重复音放宽后又收紧（区分真重复/延音） | 25 | 4 | 11 |
| 当前（无明确反证即判过） | 28 | 3 | 9 |

连续段实测：A3 A3 A3、F4 F4 F4、D4 D4 D4 整组通过。

## 5. 明天从这里继续

1. 三处"错"（D4 第2小节、E4 第4小节、A3 第5小节）：分辨是"真弹成邻音"还是
   "峰值快照被上一根弦污染导致邻音虚高"。计划：用"同一根弦同一品的延音特征"交叉验证。
2. 九处"测不准"：现在既不算对也不算错。要让它们要么给出可信度判过，
   要么明确提示"这一处没听清，重弹"。
3. 交互（用户还没谈完）：手机端信息条占地方；"要改的地方"最好能点一下跳到那一小节重练。

## 6. 踩过的坑（别重踩）

- 不要用用户的录音做每次回归（用户明确要求）。平时只跑合成回归：
  test-live / test-detect / test-product-page。需要校准数据时让用户点「导出记录」给 JSON。
- 顺序不能乱：`lastOnsetMs` 未声明这类笔误会**让整个模块加载失败 → 所有按钮失效**。
  改完必须跑 `test/test-product-page.mjs`（它在 Node 里用 DOM 桩加载产品页脚本）。
- alphaTab 这个版本**没有高亮 API** → 光标是自己画的（`#cursor` + `boundsLookup.findBeat`），
  而且**必须用谱面自己的 Beat 对象**，自己配的对象查不到布局 → 光标不显示。
- 字体必须本地：`frontend/vendor/font/Bravura.woff2` + `bravura_metadata.json`，
  且服务器 MIME 要给 `font/woff2`，否则谱面空白。
- 音色库：`frontend/vendor/sonivox.sf2`（alphaTab 仓库里那份）。
- 试听与跟弹必须互斥（sessionToken 校验延时任务），否则播放状态打架、全乱。
- **不要设"延音期"**：那是"光标停在第二个、后面每个都少一位"的根因。

## 7. 文件清单（跟弹相关）

```
frontend/index.html                产品页（谱面占大头，设置收进 ⚙）
frontend/js/follow-score.js        产品页逻辑：渲染、试听、跟弹判定、谱面标记、汇总
frontend/js/analysis.js            判定算法：estimateF0Near / chordOutsiders / 通量 / 抬头过滤
frontend/vendor/alphaTab.min.js    MPL-2.0，谱面渲染
frontend/vendor/font/              Bravura 字体（本地）
frontend/vendor/sonivox.sf2        音色库（本地）
frontend/data/hey_jude.gp3         用户给的谱面
frontend/data/hey_jude.json        时间轴（backend/tools/gp_timeline.py 生成）
frontend/data/chord_practice.json  示范和弦谱（C–Am–F–G）
backend/tools/gp_timeline.py       .gp → 时间轴（PyGuitarPro，装在 E:\VirtuCoach-Lib）
test/test-product-page.mjs         产品页自检（DOM 桩，不开浏览器）
test/test-follow-real.mjs          真机录音跑产品页逻辑（含对/错/测不准统计）
test/test-outsiders.mjs            和弦外音检测
test/cmp-tracks.mjs                核对判定时间轴与渲染谱面是否同一串音
跟弹-复盘与重新设计.md              设计复盘（游戏 vs 评估、分层设计、业界做法）
```
