# 收工状态 · 跟弹（2026-09-23）

> 这份写三件事：**今天动了什么（留 / 撤）**、**还差什么**、**下一步按什么结构做**。
> 起音层是判定的地基 —— 今天动了它，出了两个新问题，教训写在最后。

**版本号：`0923-1144`**（页面上显示 `v0923-1144`）。代码未提交，HEAD 仍是 `bf5e178`。

---

## 0. 一句话结论

今天在起音/判定这条链上做了一堆**阈值和特例**，结果是：

- **敲桌子被不停判对**（1 弦放宽用了"2kHz 抬头"当判据，而敲桌子正是宽频瞬态）；
- **分解和弦逐音收不进去**（把只在单旋律上成立的 YIN 闸门"通用"到了叠音路径）。

两条元凶都已撤。**1 弦和"一次起音过多个音"这两个问题，不是加特例能解决的，要改结构**
（见 §3，就是用户 9-21 就写下的"起音分支 + 持续段分支"两路结构）。

---

## 1. 今天保留下来的（都做过回归）

| 处 | 改动 | 备注 |
|---|---|---|
| 判定 | **弹错停下重弹**：判过才前进，判错停在原地等重弹；光标不再预先往前跳 | 用户口径 |
| 判定 | **同一个音只记第一次错**（重弹不累加错误数） | 用户口径 |
| 跟节拍 | **改成音驱动**（停用老的"时钟对号"）+ **总时间账**（第一个音→最后一个音，±10s） | 用户口径 |
| 跟节拍 | **漏拍 / 换和弦不流畅**：只在跟节拍记；"换和弦不流畅"只有和弦谱用；阈值 `max(1.5×谱面间隔, 2×拍长)` | 用户口径 |
| 跟节拍 | 节拍器 = **独立开关**（跟节拍模式也自己出声，不参与判定） | 用户口径 |
| 逐音页 | **点音格子/和弦卡从那一格开始**，并清掉旧的绿红标记与计数 | 用户口径 |
| 起音层 | **同音、离上一拨 400ms 以内**，必须有 2kHz 抬头 ≥2.0 才算新起音（`sameRepeat*`） | 用户口径"取中间值 + 重拨证据" |
| 配置 | `pluckClarityMin / pluckMinHz / pluckRetries` 从页面挪进 `CFG`（**数值不变**） | 中性 |
| 版本号 | 0922-1935 → 0923-1144（17 个文件） | 不换号手机会用缓存旧模块 |

---

## 2. 今天撤销的（都是没验证就上的，教训）

| 撤掉的东西 | 后果 | 现状 |
|---|---|---|
| "不像琴声"闸门从 Hey Jude **通用**到所有单音路径 | **分解和弦逐音全被丢**（YIN 在叠音里必然放弃）：arp 导出从"32 起音 0 丢"变成出现 `result: undefined` | 已还原：只对 `songKind === 'heyjude'` |
| 1 弦起音口子："有 2kHz 抬头 ≥2.5 就不丢" | **敲桌子不停判对**（敲桌子=宽频瞬态，2kHz 抬头巨大） | 已删 |
| 1 弦判定放宽："锚定读数 ±45 音分就放行（nHarm≥3）" | 宽频在期望音附近到处是峰 → 敲一下判一次对 | 已删 |
| 判定层"±75 音分内不被擦边对手翻案" | 让"故意弹错"算对（导出里 C4→B3、A3→G#3 都判 ok） | 已删 |
| 1 弦压电平门限（压到 45%，最低 0.02） | **邻座小声弹琴被收进来** | 已删（`onsetGateLevel` 回到 3 参数） |
| 噪声地板初始化"只吃安静帧" | 同上（房间有底噪时门限升不上去） | 已还原 |
| 光标 `mapSequenceToSlots` 两次配对改动 | 第一次自由配对 → **光标乱跳**；第二次单调配对也没验证 | **已 100% 还原**（`git diff` 为空） |

**规矩（写给以后）**：判据能通用，**适用条件不能通用**。凡是在单旋律上成立的判据
（YIN 周期性就是这个），挂到叠音路径上之前必须先用录音验证。

---

## 3. 还差什么 & 下一步的结构（用户 9-21 定过，今天没照做）

**两个未解决的问题**

1. **1 弦老是检测不到**（今天放宽过两次都出事，现在回到"1 弦没有任何特例"）。
2. **一次起音过好几个音**（11:28 导出：三下间隔 436/434/431ms，其中两个 2kHz 抬头只有 1.19 / 0.79
   —— 没有"新拨"的证据却被认成起音；同音守卫只覆盖"同音 + 400ms 内"，那次三下是不同音）。

**结构（原文见 `思路点评-2026-09-21.md`）**

> "Onsets and Frames（Hawthorne 2018）、Basic Pitch 都是'**起音分支 + 持续段分支**'两路……
> **改法**：起音那一刻只记'这里有一根弦被拨响'（分段）；音高在**起音后 30~170ms 的稳定段**量。"
>
> "短期能做的折中：起音必须**同时**满足'像一根弦在振动'（周期性强、谐波成串）才消耗音符槽。"

也就是：

- **起音分支**：只回答"这一下有没有一根弦被拨响"，**不借音高**（YIN 在起音瞬间本来就不稳）；
- **音高分支**：起音后 30~170ms 的稳定段量；
- **"像一根弦"那一关**加在**消耗音符槽**这一步上（周期性强 + 谐波成串），
  而不是加在起音那一刻的 YIN 上。

**验收标准**（必须先用录音离线跑，达标才接进产品）

- 分解和弦逐音：`6415慢速` / `6415略微快速` 全部收得进，且**不多吃**（同一次拨弦只算一次）；
- 杂音：`说话.m4a` / `咳嗽.m4a` / `周围有人弹吉他.m4a`（0.051~0.079，贴着 0.05 线）**一下都不算**；
- 真音底线：`gt-notes` 双向 **39/39**（含 ±1/±2 判错整列）；
- 技巧：击弦 / 勾弦 / 滑音 四例全对；
- 真实谱面：Hey Jude 段不退化。

---

## 4. 常用命令

```bat
cd /d E:\GuitarFollowLab
node test\gt-notes.mjs          :: 39 个真机拨弦的双向底线（必须 39/39）
node test\test-follow-page.mjs  :: 主回归（注：跟节拍有 4 条断言还是老口径，待改）
node test\check-build.mjs       :: 版本号一致性（必须 ✅）

:: 离线探针（C:\Users\Administrator\vc_gf\）
node voice-gate.mjs <f32> [标签]        :: 起音层 + 各把尺子的对照表（DIST 那行就是分布）
node pluck-audit.mjs <f32>              :: 独立数拨弦 vs 起音层报出，看漏在哪一条
node prepend-silence.mjs <in> <out>     :: 补 3.3s 静音，喂 test-follow-real.mjs
```

---

## 5. 待改的收尾（不影响手机实测）

- `test-follow-page.mjs` 里 **4 条断言仍是老口径**：跟节拍"每音时间偏差/抢拍拖拍"3 条 +
"拖 1.2 个音距就该按错算" 1 条 —— 跟节拍改成音驱动后按新口径改写。
