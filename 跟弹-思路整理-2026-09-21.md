# 跟弹：思路整理（2026-09-21 收工）

> 今天改了十几轮，越改越糟。用户要求：**先别改代码，把思路理清**。
> 这份只讲三件事：**数据说明什么 / 哪里是我改坏的 / 思路本身有没有问题**。

## 一、三份导出记录的对比（数据其实在变好）

| | 11:26 那份 | 16:07 那份 | 16:20 那份（最新） |
|---|---|---|---|
| 谱面×时间轴对齐 | 没测 | **失败**：拍点时刻全是 `NaN` | ✅ **成功**：偏差 0.04ms、对上 82/118 个音 |
| 量音高 | 老是 180~220Hz（锁到别的弦） | 同上 | ✅ **准了**：12 个音里 6 个偏差 ≤4 音分 |
| 判定 | 37 对 / 1 错（但期望音全是 C4，等于没判） | 25 错 / 2 对 | 5 对 / 7 错 |

**结论：两个最基础的东西现在是通的**——对齐通了、音高量准了。剩下的问题不在"听不听得出"，在别的地方。

## 二、最新的 12 个音，逐条看

```
#1  期望C4   量到C4   差   4音分   ok
#2  期望C4   量到A3   差-257音分   bad   ← 差两个半音
#3  期望A3   量到A3   差  -1音分   ok
#4  期望A3   量到A3   差  +1音分   ok
#5  期望A3   量到G3   差-250音分   bad   ← 差两个半音
#6  期望A3   量到G3   差-196音分   bad   ← 差两个半音
#7  期望C4   量到D4   差+224音分   bad   ← 差两个半音
#8  期望G3   量到G3   差  +4音分   bad   ← **量对了却判错（bug）**
#9  期望G3   量到G3   差  -3音分   ok
#10 期望G3   量到A3   差+198音分   bad   ← 差两个半音
```

两件事一下就看清了：

1. **#8 是纯 bug**：量到的音对（G3，差 4 音分），却判错。原因是"可信度"那道关卡
   （`domSharp > 1.02`）卡掉了它（记录里 `domSharp=1.01`）。**一行就能修。**
2. **6 个音差得几乎一样（180~260 音分 ≈ 两个半音）**，而且**方向一正一负**——
   这不是"量错了"（量错不会这么整齐），而是**"期望的音"配错了**：
   把用户弹的 A3 配到了谱面的 C4 上。

## 三、为什么会"配错音"：这是思路层面的问题，也是今天最该记住的一条

现在的绑定方式是：**"听到一个起音，就吃掉谱面的下一个音"**（第 1 个起音 ↔ 第 1 个音、
第 2 个 ↔ 第 2 个 ……）。

这个绑定的前提是：**每一次起音都恰好对应谱面的下一个音，一个不多、一个不少**。而现实里：

- **漏检一次**（某个音太轻、或者被余响盖住）→ 后面**所有音整体错开一位** →
  于是"弹对的判错、弹错的判对"；
- **多检一次**（同一个音被算了两下、琴弦的二次瞬态）→ 光标**一次跳好几格**。

**这两个症状正好就是用户今天报的**："音都听不到了"（漏检）、"一个音走多个"（多检）、
"弹对的判错"（错开）。**它们不是三个问题，是一个问题的三种表现。**

### 正确的绑定方式（下一步要做的）

不要用"顺序"绑，用**时间 + 音高**绑：

1. 谱面每个音都有它在曲子里的时刻（对齐已经通了，这份数据已经有了）；
2. 起音出现时，去找**时刻上最接近、而且音高也说得通**的那个音 —— 而不是"下一个没判过的"；
3. 判过的音做标记；漏了就是漏了，**不会把后面的音整体带偏**；
4. 光标跟着"时间"走，而不是跟着"判了几个"走。

这样做之后：漏检一次只丢一个音；多检一次（同一个音两下）会落到同一个音上、不会跳格。

## 四、我今天改坏的地方（复盘，别重复）

1. **把"测不准"这一档取消了** → 量不准的读数直接变"错"。用户要的是"对就是对、错就是错"，
   但代价是**可信度关卡一卡，正确的音也变错**（#8 就是这样）。正确做法：先修可信度关卡，
   再取消这一档 —— 顺序反了。
2. **门槛一轮一轮往上调**（0.006 → 0.02 → 0.05 → 0.10）：往上调压住了噪声，但也开始
   **漏掉真弹的音**（这次 12 次起音里漏了大部分）。门槛不该"往上试"，该**用数据定**：
   用户录的那几段里，"真弹的电平"和"环境的电平"分布是能直接量出来的。
3. **我的合成测试不可信**：合成出来的拨弦电平只有真机的 1/10，所以我按合成信号调的参数，
   到真机上等于没调。**今天所有"测试全绿"都建立在这个假信号上** —— 这是最大的教训。

## 五、思路本身有没有问题？

**没有。** "已知谱面 → 只回答'这一个音是不是谱上那个音'"这条路是对的（今天的数据也证明了：
量音高在真机上能做到 4 音分以内）。有问题的是**绑定方式**（按顺序）和**参数的定法**（靠试）。

但如果第 3 节那条不改（仍然按顺序绑），那就**别继续做**——因为漏检/多检在真实演奏里
一定会发生，按顺序绑一定越走越偏。这条是"要么改、要么不做"的分水岭。

## 六、建议的下一步（按顺序，一次只动一件事）

1. **修 #8 那个 bug**（可信度关卡 `domSharp > 1.02` → 只要有一根立得住的线就算可信）。
2. **换绑定方式**：从"按顺序"改成"按时间 + 音高就近匹配"（第 3 节那四条）。
3. **门槛用数据定、不再往上试**：拿用户那 6 段录音，量出"真弹电平"和"环境电平"的分布，
   取一个分得开、且离两头都有余量的值，写进测试当靶子。
4. **把合成测试的电平校准到真机同一量级**（已经做了 ×8），以后凡是"门槛/电平"相关的改动，
   必须同时跑合成 + 真机录音两条。

前三步做完之前，**不要再动判定算法**——今天已经证明：在没有可靠评估回路的情况下改算法，
只会把能用的部分也弄坏。
