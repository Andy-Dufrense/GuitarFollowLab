# 收工状态 · 2026-09-23 夜（明天从这里接，别再犯今天的错）

> 用户要求：**今天最完美的状态、他的铁律、他的原话思路、我骂过的错、每个方向该怎么做**，
> 全部写在这里。**新窗口先读这一份 + `AGENTS.md`（铁律）**，不许再从零试。

---

## 0. 今天最完美的状态（版本 v0923-1915）

| 验收 | 结果 |
|---|---|
| `gt-notes` 双向（39 个真拨弦） | **39/39** ✅ |
| 真机 击弦 / 勾弦 / 滑音 | **3/0、2/0、4/0** ✅ |
| 逐弦（arp + `C-T3231323.f32`，用户弹对的录音） | **对 4 / 错 1** ✅ |
| 一直弹 1弦1品（错音必须挡） | **对 0 / 错 1** ✅ |
| 合成主回归 `test-follow-page` | 6 项（跟节拍老口径那几条 + 2 项合成读数变少） |
| `check-build` | ✅ 17 个文件统一 |

**这一版里判定链（有效）**：

1. **起音层**：电平门限（`absNeedMin 0.05`，环境×6）+ 够陡（1.4×）+ 形状变了 + 冷却（按谱面间距分档，快段落 ≤250ms 时上限 85ms）+ 同音重拨要有 2kHz 抬头（`sameRepeat*`，当前 `sameRepeatTightMs: 0` = 不生效）。
2. **"像不像一根弦"闸门**：`clarity > 0.42` **或** 谐波墙 ≥ 阈值（`CFG.pluckHnrMin`，当前 2.0），1 弦格放宽到 0.25。**对所有单音路径生效**。
3. **读数（不看答案）**：起音前后各 **40ms** 相减（只留抬头）→ 零填充到 8192（5.9Hz）→ 取**最低的成串线**（k=1..4 都立着，且**基频那条线 ≥ 谱峰值的 30%**）。
4. **判定**：**音名一样就过**（检测到的音名 == 谱面那个音；C4/C4 过，C4/C3、C4/C#4 都不过）。
   技巧整对豁免（一次起音 + 按 BPM 等第二个音）；读数为空时退回"候选重排 + 轻音兜底"。
5. **候选重排**：本音失配 < 250 且领先最强邻居 **≥10%**（`rivalMargin 0.90`，原来 3% 太松）。
6. **判错停下**：判错不前进、标红、等重弹；同一个音只记第一次错。
6. 显示用**同一个音名**（`heard` 那行显示检测到的音名 + "（和谱面一致）"或"（谱面要 X）"）。

**导出里现在带的字段**（排查全靠它）：`expect / measured / detHz / detCents / candFit / candMargin / passCand / quietOk / str1 / step / stepUsed / stepOk`，另有 `nearMiss`（电平过门限却没被认成起音的帧 + `why`）。

---

## 1. 铁律（`AGENTS.md` 里也有一份，这里再钉一遍）

1. **阈值一次只动一个量，幅度要小**（0.05~0.5 的粒度，比如 0.3 → 0.35 → 0.41）。每档都要给用户两边的数字（真音掉几个 / 杂音进几个），让他挑。
2. **起音层是地基**：没有离线验证不许动；动完必须给"真音没丢、杂音没进"两组数字。
3. **判据能通用，适用条件不能通用**（YIN 的 clarity 在叠音里必失效 —— 把"不像琴声"闸门通用到分解和弦就是这么坏的）。
4. **不许用能自证的判据**：锚在期望音上的读数、`nHarm≥3`、单个"2kHz 抬头"。敲桌子/说话/邻音都能满足。
5. **只动用户点名的东西**。没点名的一律不许"顺手改"（今天擅自改光标、擅自改候选表，都被骂了）。
6. **特例不许按歌名/测试页写**；要通用就真的通用，但不能把不成立的判据硬挂上去。
7. **改完必须报**：改了哪个文件、哪个函数、动了哪个值、跑了哪些回归、数字多少。过不了就说"没过"。
8. **不许夹带**：说了"恢复成刚才那版"就只做恢复，不许顺手带上别的改动。
9. **显示必须和判定用同一个结论**（否则会出现"显示同一个音名却不给过"这种自相矛盾）。
10. **不确定就问一句**，不要替用户做产品决定（比如"把绝对门限抬到 0.08"会改他弹琴力度，必须他定）。

---

## 2. 用户的原话（他的思路，逐条抄）

**关于起音与衰减：**

> "我的起音对于一个在折线图中减弱的音的逻辑是不做判定的"
>
> "每一个音符的起音就只单独判定这个音符的音准，在窗口内比音符声音小（折线低）或在减弱的音都不做处理"
>
> "上一根弦的余波，在这次起音是下降的，我们只计算起音的那条线"

**关于窗口：**

> "不固定窗口看起音，而是在每一次起音的周围定窗口，在这个窗口内判定音准，这样也可以避免一个音过去两个相同音符"
>
> "起音要有界限，不能把周围的杂音噪音都当作起音"
>
> "窗口不也要根据音符和 bpm 来随时更改吗"

**关于判对判错：**

> "识别出音准之后，和期望音判断，如果在容忍度范围内就判对，不在容忍度内，不用管是不是差了 1 品 2 品 12 品，只要不是容忍度内就判错"
>
> "这要是同一个音名就给过，C4 和 C4 就过，C4 和 C3，只要不是他自己，就不过" ← **最新定版口径**
>
> "容忍度还是设置为 80 吧，50 低了"（后来改成按音名）

**关于"我们知道谱子"：**

> "我们知道谱子，所以我们可以设计，面对 1 弦的地方，宽容度放开一点让一弦进来，面对技巧，只判断一次起音，根据不同 bpm 设置不同等待时间，我们是可以分析并且知道谱子的"
>
> "和弦逐音检测那里……要和单音一样逐弦判断"
>
> "双音和三音我们能做到判断单个的正误是最好的"

**其他关键立场：**

> "不要钻邻座的牛角尖了，只要我们能做到干净检测就好了"
>
> "1 弦 1 品都按照我的思路过了，为什么同样的逻辑 1 弦 3 品可以过全部 C 和弦"
>
> "4Hz，那么低，我们能收进来就是我们的问题了"
>
> "1 弦的解决办法是，看到谱子上有一弦的地方，就放宽限度，仅在这些地方放宽，不要在其他地方也放宽"

---

## 3. 今天被我搞坏过、明天绝对不要再犯的（用户骂过我的）

| 我干的事 | 后果 | 正确做法 |
|---|---|---|
| **擅自改光标**（自由时间配对 / 单调配对） | 光标乱跳、"一个音过好几个音"的错觉 | 光标只按 `noteBeats` 下标走；要改必须用户点头 |
| **给 1 弦压低电平门限**（到 0.02） | 邻座小声弹琴、敲桌子全被收进来 | 1 弦只在**判据线**上放宽（clarity 0.42→0.25 之类），**电平门限不动** |
| **把"不像琴声"闸门通用到分解和弦** | 逐弦真音被逐个吞掉 | 判据能通用、适用条件不能通用；要在叠音里立得住才挂过去 |
| **用 2kHz 抬头 / 锚定读数当放行证据** | 敲桌子不停判对 | 只用"不看答案 + 成串"的证据 |
| **把"按音分判"接到退化的差分测量上** | `|cents| ≤ 80` 恒成立 → 什么都判对 | 音分规则必须配**可靠读数**；读数不可靠就不上 |
| **"起音台阶"当硬闸门**（1.5 阈值） | 一批"音分 ±1~12"的真音被判错 | 只在**轻音**上用；后来整条删掉 |
| **拿技巧格去验起音逻辑** | 得出"这套不行"的错误结论 | 技巧是**整对豁免**的特例，不能拿它验起音 |
| **读数的窗长用期望音频率去定** | 等于把"期望音那把尺子"又带回来 | 窗长用固定值（40ms）或**物理先验**（吉他音域），不用期望音 |
| **"抓不到读数就不算过"** | 把逐弦正确演奏误杀（4/1 → 0/1） | 没有读数 → 退回原链路，不硬判 |
| 用 PowerShell `>` 重定向写源文件 | UTF-16 乱码、语法错 | 改文件只用 `apply_patch`；批量重写要用 UTF-8 无 BOM |

---

## 4. 每个方向现在到哪一步、下一步怎么做

### 4.1 起音层（不动结构，只小步调）

- **现状**：门限 0.05（试过 0.052/0.055 → 掉真音，已撤回）；冷却期已按谱面间距分档（快段落 ≤250ms → 上限 85ms，治"快弹漏音"）。
- **待验证**：用户手机快弹一段 + 导出 → 看 `nearMiss` 里还有多少"冷却中 / 不够陡"。
- **下一步**：若"不够陡"仍多 → 让 2kHz 抬头那条接管（`hfBandRiseMin` 2.5 → 2.2 一小步）。

### 4.2 读数层（今天的重点，正在做）

- **现状**：起音前后各 40ms 相减 → 只留抬头 → 零填充 8192 → 最低成串线（k=1..4 + 基频线 ≥30% 峰值）。已治住"1弦1品全过"，也已让逐弦读数稳定在 G4。
- **待验证**：1弦空品 E4 是否还会被读成 F2（用户报的）；空读数（`detHz=-`）还多不多。
- **下一步**：若还锁低频垃圾 → 把相减窗口做得更干净（pre 用"起音前 10~40ms"、post 用"起音后 0~30ms"）；顺序始终是**先修读数，再谈判定**。

### 4.3 判定层（用户已定版）

- **音名一样就过**（检测到的音名 == 谱面音）；技巧整对豁免；判错停下；邻居翻案要 ≥10%。
- **不许**再自己改这一层的口径 —— 要改先给用户两种写法 + 两边数字。

### 4.4 双音 / 三音

- **现状**：同一时刻的音算一格；成组判（解释率 ≥0.7 + 每个音的谐波墙 ≥2.0）。
- **已知**：茉莉花那格三音（`9 11 11.m4a`）目前 **对 0 / 错 1**，阈值要扫（0.7→0.6、2.0→1.5，一次一个量）。

### 4.5 光标

- **根因已找到**：`#cursor` / `#marks` 原来放在 `<div id="score">` 里，**alphaTab 渲染时会把它们删掉** → `getElementById('cursor')` 取不到 → 一个光标都画不出来。
- **已改**：两个 div 移到 `#score` **外面**（挂 `#scoreWrap`，并补 `#score` 相对 `#scoreWrap` 的偏移）；渲染结束时补跑映射 + 重摆光标。
- **待验证**：手机上切茉莉花↔Hey Jude 来回切，光标是否都在。

### 4.6 还没做的收尾

- `test-follow-page` 里 **跟节拍那 4 条老口径断言**要按"音驱动 + 总时间"改写；
- 茉莉花/双音三音的**离线用例**（补进回归，别再靠猜）。

---

## 5. 明天第一件事（就这三条，别扩散）

1. 让用户用 **v0923-1915** 验：**1 弦空品 E4 会不会被读成 F2**、逐弦弹 1弦3品会不会过、Hey Jude 正常弹会不会被误杀；
2. 拿他新导出的 `detHz / detCents / nearMiss` 三样，**只改读数层**（窗口/成串判据），一步一个量；
3. 每次改完跑这四组：`gt-notes` 39/39、真机 击弦/勾弦/滑音 3/0、2/0、4/0、逐弦（`C-T3231323`）不许掉、`1弦1品` 必须 对0。

**一句话总结今天的教训**：**病根在"读数不可靠 + 判定用了会自证的证据"**；所有翻车（1弦特例、音分、丢起音、敲桌子、邻座琴、台阶）都是这两个派生出来的。修的时候只动一处、只动一个量、每步给数字。
