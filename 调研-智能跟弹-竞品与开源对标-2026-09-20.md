# 智能跟弹：竞品调研 · 开源对标 · 与我们现有链路对比

> 写于 2026-09-20。对象：`E:\GuitarFollowLab`（跟弹原型）+ `E:\VirtuCoach`（分析/反馈链路）。
> 调研方式：能打开的一手页面直接抓（官方页/arXiv/GitHub 源码与 LICENSE），打不开的标「待确认」，不靠印象下结论。

---

## 0. 三句话结论

1. **慢练那条链路（闭集候选音谐波比较）是这套东西里最值钱的部分，别推倒重来。** 同类产品要么限制输入
   （有线/单音优先），要么直接上神经网络做开集转写；「已知谱面 → 只判是不是谱上那个音」这条路既省算力又抗音色，
   方向是对的。
2. **原速跟弹缺的不是"更好的音高检测"，是时间层。** 我们现在是"每音一判、判过才走"的闸门式结构，
   没有"我现在认为你弹到哪了 + 你现在是快是慢"这个状态，140BPM 上不去是架构问题，不是参数问题。
   学术界/工业界的标准件叫 **score following**：状态 = 谱面位置 + 速度，用在线 DTW / HMM / 粒子滤波持续更新。
3. **三份可以直接抄的作业**：`web-score-following`（MIT，浏览器里跑 OTW，含全部参数）、
   `alphaTab`（MPL-2.0，GP 3-8 解析 + 渲染 + 播放 + 光标）、`basic-pitch`（Apache-2.0，轻量复音转写，含 TS/TF.js 版）。
   **许可红线**：Essentia / Essentia.js（AGPL-3）、aubio（GPL-3）、MuseScore（GPL-3）、Chordino（GPL-2）、flippy（GPL-3）
   不能进闭源产品。

---

## 1. 我们现在的链路（先把事实钉死）

### 1.1 实时侧（GuitarFollowLab，慢练/过关式）

```
麦克风 → AnalyserNode 取 16384 点时域（48k 下 341ms）
  ├─ 起音：① 电平 > 48ms 前 1.6 倍  ② 频谱通量 > 0.16 且电平 > 48ms 前 1.15 倍；两者都过噪声门限（地板×4）
  ├─ 逐频点本底（上升 8% / 下降 25%）→ novelSpectrum = max(0, 当前 − 本底)   ← 把余响吸走，只留"新出现的"
  ├─ 单音判定：对候选音（目标 + 0,±1,±2,±5,±12 半音）做谐波打分（1~10 次谐波、权重 1/√k、±40 音分容差、
  │            琴弦刚性 B 取 [0, 0.0002, 0.0005, 0.001] 里最好的）→ 目标/次优 > 1.03 判过
  ├─ 和弦判定：12kHz 降采样 → FFT 4096（341ms）→ Chroma → 与模板余弦相似度（门限 0.72）
  └─ YIN：只留作滑音轨迹显示和音分读数，判定不用它
```

参数（`public/js/config.js`）：结算等待 60ms、通量窗 2048 点（43ms）、最小间隔 90ms、和弦结算 300ms、判错停留 300ms。

### 1.2 离线侧（VirtuCoach）

上传视频 → 抽音频 → （可选）公司音轨分离 API 取吉他/人声轨 → 和弦/节奏/技巧分析 + 手型视觉分析 → LLM 生成反馈报告。
这条链路是**离线批处理**，与实时跟弹的约束完全不同（可以等几秒、可以用大模型、可以重算）。

### 1.3 已经能做到 / 做不到（来自 HANDOFF 与 README，未改动）

| | 现状 |
|---|---|
| 六弦空弦 + 按弦音音高 | ±1~3 音分（合成信号） |
| 音色无关性（5 种音色） | 整段通过（这是验收标准，G 组） |
| T3231323 @ 60/80/100/120 BPM | 通过 |
| @ 140 / 160 BPM | 过不去（八分音符 214ms / 188ms） |
| 原速跟弹（不停、只标对错） | **完全没做** |
| 低音弦 + 和弦余响 → 判高八度 | 未修（合成可复现） |
| 真机录音验证 | 没做过，全部是合成信号 |

**结构性问题一句话**：现在的算法只回答"这一个音对不对"，不回答"你在曲子的哪个位置、快还是慢"。

---

## 2. 竞品扫描

按"输入方式 + 判定方式 + 跟弹形态"分类。标注 ✅ = 官方页面/论文可查，⚠️ = 产品常识或有待确认。

| 产品 | 形态 | 输入 | 跟弹/判定做法 | 对我们的意义 |
|---|---|---|---|---|
| **Yousician** | 手机/桌面 App | 麦克风（也可接音频接口） | 实时判定细节不公开。**可查的是内容侧**：其研究者 Lukoianov & Klapuri 的论文（arXiv 2510.05756）用「近似分轨 → MERT 预训练骨干检测扫弦 → 用专家词表解码节奏型」 把歌曲转成谱，数据集是 410 首流行歌 + 专家按谱录的 cover ✅ | 说明头部玩家把"歌 → 谱"当核心难点投研发；实时侧多半也是 ML + 时间对齐，不是单音 YIN |
| **Rocksmith+ / 2014** | PC/主机/手机 | 有线（音频接口/Real Tone Cable）为主 ⚠️ | 官网原文："continuously tracks and analyzes your performance, while providing instant recommendations" ✅。传统上手用有线输入压噪声、配延迟校准 ⚠️ | 有线 = 可以按弦分带做 per-string 检测；我们只有麦克风，难度高一个量级 |
| **Simply Guitar（JoyTunes）** | 手机 App | 麦克风 | 官网可查的是调音器（麦克风测音）✅；跟弹判定未公开 ⚠️ | 同场景对照：麦克风 + 手机端算力能做到什么程度 |
| **MakeMusic Cloud（原 SmartMusic）** | 学校音乐教育 | 麦克风 | 学生跟伴奏演奏、系统给音准/节奏评估（产品主页可查）✅ | "作业 + 评估"这条业务形态和我们最像，可参考它的报告口径 |
| **Soundslice** | 网页 | 无麦克风判定 | 谱面 + 真实录音同步、音符高亮、点任意音符跳转、变速不变调、循环、分轨、移调 ✅ | **跟弹的"另一半"标准答案**：谱面时间轴 + 播放 + 光标，先把这个做好 |
| **Songsterr / Ultimate Guitar** | 网页/App | 无判定 ⚠️ | 网页谱播放器：光标 + 变速 + 循环 ⚠️ | 同上，谱面交互的基线体验 |
| **Guitar Pro 8** | 桌面 | 无判定 ⚠️ | 谱面播放 + 速度训练器/循环/节拍器（官网当前不可达，细节待确认） | alphaTab 就是它的开源等价物，可替代 |
| **Chordify** | 网页/App | 音频（不是麦克风） | 任意歌曲 → 和弦谱，随播放高亮 | 和弦级跟弹的轻量形态，判据比音符级宽松得多 |
| **Poputar（视感科技）** | 智能吉他 | 蓝牙 + LED 指板灯 | 官网原文："通过蓝牙点亮琴颈的 LED 灯闪，与 App「音乐CP」的游戏曲谱与互动课程结合" ✅ | 国内主流路线是**灯指路**，把"我在哪"外化成视觉提示，不依赖音频识别 |
| **GEEK 智能吉他（弹琴吧）** | 智能吉他 | LED + App | 官网原文："吉他上加装了 LED 灯……在灯光和伴奏的指引下，能迅速找到下一个和弦或按法"；弹唱/独奏/耍酷三模式 ✅ | 同上；注意它是"指引"不是"判定" |
| **Fret Zealot** | 指板 LED 贴条 + App | App | LED 指法提示 + 课程/歌曲 ✅ | 硬件路线对照 |
| **小叶子 / The ONE（智能钢琴）** | 智能钢琴 + App | 麦克风 | 官网原文："AI 精准纠正错音错节奏" ✅ | 国内"AI 陪练"的口径就是**错音 + 错节奏**两项，跟弹验收指标可对齐这个 |
| **Enya 自家** | 智能吉他 + App | — | （本次未拿到可比对的公开技术口径） | 内部信息补 |

**竞品的共同点**：

- 都把"**我在哪 / 我下一步弹什么**"用视觉（光标、灯、瀑布流）解决，音频识别只负责"对不对"；
- 都用**明确的校准/开始流程**换来稳定的判定（调音、延迟校准、导数、起拍）；
- 报告口径基本是「音准 + 节奏 + 流畅度」，与我们 VirtuCoach 的音准/节奏/技巧三维度天然对齐。

---

## 3. 可学的开源项目（附许可证，红字是雷）

### 3.1 时间对齐（我们最缺的那一层）

| 项目 | 许可 | 能学到什么 |
|---|---|---|
| **matthewcaren/web-score-following**（MIT） | 可商用 | **最贴近我们的一份作业**。MIT + Web Audio Conference 2024，MIT Media Lab 的 ConcertCue 项目。参考特征用 CENS（hop 4096 @44.1k ≈ 93ms，n_fft 8192），实时侧流式算 Chroma（hop 3686），用 **Dixon 的 OTW（在线时间规整）** 维持位置，参数 `c=300, max_run_count=3, diag_weight=0.4`，输出 0~1 的谱面位置。整套用 Pyodide 跑在浏览器里（`web/public/python/otw-*.py`，核心就 4 个短文件） |
| **seingreen/STRAdi**（Apache-2.0，ISMIR 2026） | 可商用 | 论文就是 *Real-time Violin Transcription for Score Following*：**完全因果（不看未来音频）**的流式转写模型 + 对齐。想上神经网络跟弹时，照它「因果性」的设计约束做，别拿离线模型硬套实时 |
| **flippy-fyp/flippy**（GPL-3） | ⛔ 不能进闭源 | CQT + DTW 的谱面跟随实现，读代码可以，抄进去有 GPL 风险 |
| **CPJKU/partitura**（Apache-2.0） | 可商用 | 记谱数据 IO + 对齐工具，适合做评测/标注管线（把 .gp / MusicXML / MIDI 统一成对象再离线算指标） |
| 经典方法（有论文无代码） | — | **Dixon 2005 在线时间规整（OTW）**、**Raphael 2006 粒子滤波谱面跟随**、IRCAM **Antescofo**（工业级、商业化）。跟弹的技术词汇就是这些 |

### 3.2 谱面 / 时间轴（跟弹的另一半）

| 项目 | 许可 | 能学到什么 |
|---|---|---|
| **CoderLine/alphaTab**（MPL-2.0） | 可作库商用（改了它的文件要回馈） | 直接把 Guitar Pro **3-8**、MusicXML、alphaTex 读进来，渲染成谱/六线谱，内置 alphaSynth 用 Web Audio 播放，支持**变速、循环、移调、光标、与真实音视频同步**。官方文档原话有 "live display cursor" 和 "Synchronize songs with real audio or video recordings" |
| **Perlence/PyGuitarPro**（LGPL-3） | 服务端用较安全 | 只读 .gp3/4/5（+ 部分 gpx），把音符/时值/力度/技巧/速度/反复变成 JSON。我们只想要"时间轴"的话，这条路最省事 |
| **TuxGuitar**（LGPL-2.1）/ **MuseScore**（GPL-3） | Tux 谨慎可用；**MuseScore 只能读** | **反复、连音、三连音、速度变化**这些谱面语义的正确处理方式——自己写时间轴最容易在这里翻车。MuseScore 是 GPL，不能进闭源产品 |
| **OSMD / VexFlow**（MIT / BSD） | 可商用 | 如果走 MusicXML 而不是 .gp，用它们做渲染 + 光标 |

### 3.3 复音 / 转写（提升速度上限的备选）

| 项目 | 许可 | 备注 |
|---|---|---|
| **spotify/basic-pitch** + **basic-pitch-ts** | Apache-2.0 | 轻量复音转写，带 pitch bend，有 TF / ONNX / CoreML / TFLite / TF.js 版，浏览器可跑。**目前最现实的"上神经网络"选项** |
| **cwitkowitz/guitar-transcription-continuous（FretNet）** | MIT | 吉他**按弦/按品**的转写与指标（六个字符串级别），配 `mir_eval`。注意：它训练/评测在 GuitarSet 的**六路拾音器**数据上，单麦克风场景不能直接套 |
| **marl/GuitarSet** | MIT | 吉他转写标准数据集（Zenodo 下载），同时也是"我们该怎么建自己的评测集"的模板 |
| **magenta/mt3**、**Onsets and Frames**（magenta） | Apache-2.0 | 多乐器/钢琴系转写的架构参考；MT3 有吉他音色程序 |
| **marl/crepe** | MIT | 单音高很强，但**只支持单音**，不适合复音场景 |
| **librosa** | ISC | pyin/onset/CQT/DTW 齐全，离线分析最省心 |
| **CPJKU/madmom** | 源码 BSD，**预训练模型另有学术用途限制** | RNN 起音/节拍/判拍，做离线标注和评测的黄金标准；模型许可要单独看 |
| **aubio** | GPL-3 | 起音/音高/节拍的老牌 C 库，GPL 传染，只能学思路 |

### 3.4 和弦 / 分轨

| 项目 | 许可 | 备注 |
|---|---|---|
| Chordino / NNLS-Chroma | GPL-2 | 教科书级和弦识别；我们自己的 Chroma + 余弦已经够用，且没有许可负担 |
| demucs / spleeter | MIT | 分轨参考；VirtuCoach 已接公司音轨分离服务，离线侧不需要自建 |

---

## 4. 逐环节对比：谁的做法更合理

| 环节 | 我们 | 主流做法 | 判断 |
|---|---|---|---|
| 起音检测 | 电平抬升 **或** 频谱通量 + 噪声门限 | 频谱通量 / 神经网络（madmom） | **我们的更合理**：两条判据互补，正好覆盖"余响不消音"和"环境噪声"两个真实场景，成本远低于 RNN |
| 单音判定 | 已知目标 → 候选音谐波比较（闭集、相对比值） | 开集音高检测（YIN/pYIN/CREPE）后比对 | **我们的更合理**：不吃音色/换琴，不需要用户先标定。文献里这叫 score-informed，正是"有谱信息要用足" |
| 复音 / 多弦同时 | 无 | 有线输入做 per-string，或用 ML 转写 | **我们落后**。但不必一步到 ML：先做"六条弦各自的频带 + 谐波能量"这种闭集分带跟踪，成本低、和现有哲学一致 |
| 和弦 | 12kHz + 4096 点 FFT → Chroma → 余弦 | 同上（Chordino 系） | 持平，够用；低音/转位不判这个取舍是对的 |
| **时间对齐** | **完全没有**（每音一闸门） | 在线 DTW / HMM / 粒子滤波（score following） | **我们必须补**。这是"原速跟弹"和"120~140BPM 天花板"的同一个根因 |
| 谱面时间轴 | 无（练习内容是硬编码的几个模式） | .gp / MusicXML → 音符事件表 + 拍点 | 必须补，且是**独立于音频算法**的一条线，可以并行做 |
| 延迟预算 | 起音→测量 60ms，通量窗 43ms，和弦 300ms | 参考实现特征帧 ~84~93ms；商业产品普遍做"输出延迟校准" | 单音 ~100ms 可接受；跟弹模式下要**把判定和显示解耦**（先闪提示、后出结论），并加一次延迟校准 |
| 评测 | 全合成信号 | 自有标注集（Yousician 用了 410 首歌 + 专家录制） | 我们最缺的是**真机录音**。HANDOFF 已经把这条列为优先级 1，方向正确 |

### 一句话回答"哪个更合理"

- **慢练模式**：我们现在的链路**比市面上大多数做法更合理**（闭集相对打分 + 抗音色），保留，只补它的已知 bug（低音弦判高八度）和真机验证。
- **原速跟弹**：我们现在的链路**不合理**，因为它把"时间"这个维度整个省掉了。商业产品和学术界的共同做法是"**一个观测模型 + 一个时间模型**"：
  我们已有的算法正好是那个观测模型，缺的是时间模型。所以不是"推翻重来"，是"在 judge.js 旁边加一个 tracker.js"。
- **纯端到端神经网络**（Basic Pitch / MT3 一把梭）：**现阶段不划算**。手机端算力/延迟/模型体积、以及"闭集信息白白浪费"这两点都不占优。合理位置是**离线复核通道**（练完再算一遍，进报告），不是实时判定。

---

## 5. 推荐的目标架构

```
谱面 .gp ──(PyGuitarPro / alphaTab)──► 时间轴 JSON：音符/和弦/技巧 + 拍点 + 速度变化
                                        │
麦克风 ──► 帧级特征 ──► 观测模型（现有闭集谐波打分 + 每弦频带能量）──► 每个候选位置的似然
                                        │
                                        ▼
                          时间模型 tracker.js：状态 = (谱面位置, 速度)
                          在线 DTW（起步）→ HMM / 粒子滤波（速度变化大时）
                                        │
                                        ▼
              UI：光标（我在哪）+ 对错标记（不停）+ 慢速/原速切换 + 延迟校准
                                        │
                                        ▼
              练习结果（音符事件 + 时间偏差）──► VirtuCoach 现有反馈链路出报告
```

### 分阶段落地

| 阶段 | 做什么 | 为什么先做 |
|---|---|---|
| **P0** | 真机录音（手机录 T3231323 / 扫弦）+ 把合成测试和真实信号并跑 | 现在所有结论都建立在合成信号上，这是最大的风险敞口 |
| **P1** | 谱面时间轴（.gp → JSON）+ `tracker.js`（OTW，抄 web-score-following 的参数起步）+ 光标 UI | 跟弹的最小可用版本；慢练模式原样保留成 "wait mode" |
| **P2** | 按弦频带跟踪（六根弦各自的基频/谐波能量），拔高速度上限 | 比上神经网络便宜得多，且贴合"闭集"哲学 |
| **P3** | 开场标定（逐弦基频偏差 + 谐波模板 + 房间噪声底） | HANDOFF 里已设计，做完判定更稳 |
| **P4** | Basic Pitch / basic-pitch-ts 做**离线复核**，结果并进报告 | 拿到 ML 的收益，不承担实时延迟和算力风险 |

---

## 6. 许可证清单（决定"能不能抄进产品"）

| 许可 | 项目 | 结论 |
|---|---|---|
| Apache-2.0 / MIT / ISC | basic-pitch、basic-pitch-ts、librosa、CREPE、MT3、Onsets and Frames、FretNet、GuitarSet、web-score-following、STRAdi、partitura、demucs、spleeter | 可商用（保留版权声明） |
| MPL-2.0 | alphaTab | 可作库商用；**修改它自己的文件**需要回馈源码 |
| LGPL-2.1 / LGPL-3 | PyGuitarPro、TuxGuitar | 服务端/动态链接较安全；不要静态改进去 |
| ⛔ GPL-2/3、AGPL-3 | Chordino、aubio、MuseScore、**Essentia / Essentia.js**、flippy | 不能进闭源产品。**Essentia.js 最像"直接拿来用"的陷阱**：浏览器端 DSP + 和弦/节拍/音高算法很全，但它是 AGPL-3 |
| 混合 | madmom | 源码 BSD，预训练模型另有学术用途限制 |
| 未声明 | pitchfinder（npm 标 MIT，仓库无 LICENSE 文件） | 用之前先核实 |

---

## 7. 需要确认的事 / 下一步

1. **输入形态**：最终是"手机麦克风"还是"支持接音频接口/线缆"？这直接决定要不要做 per-string 检测，也决定跟弹的难度上限。
2. **真实 .gp 素材**：HANDOFF 里"等真实 .gp 文件"这条还挂着；同时要确认谱面是否包含反复、速度变化、多轨（这些是时间轴实现里最容易翻车的地方）。
3. **跟弹的验收口径**：建议对齐"错音 + 错节奏"（国内 AI 陪练的通用口径），并明确"允许用户自己控制速度"和"严格按原速"两种模式的成败标准不同。
4. **竞品结论的置信度**：本文中 Yousician（arXiv 2510.05756）、Rocksmith+、Poputar、GEEK、Fret Zealot、小叶子、Soundslice 的表述来自可访问的官方页面/论文；标 ⚠️ 的部分是行业常识或推断，需要产品同学以实机体验确认。

---

## 8. 信息来源

- Guitar Follow Lab 自身：`README.md`、`HANDOFF.md`、`public/js/{config,analysis,judge}.js`
- Yousician 内容侧论文：arXiv:2510.05756（Lukoianov & Klapuri），代码 `github.com/YousicianGit/rhythmic-pattern-transcription`
- Rocksmith+ 官方页：`ubisoft.com/en-gb/game/rocksmith/plus`（"Real-time feedback … continuously tracks and analyzes your performance"）
- Soundslice 功能页：`soundslice.com/features/`
- MakeMusic Cloud：`smartmusic.com`
- 视感科技：`poputar.com`；弹琴吧 GEEK 智能吉他：`tan8.com/guitar`；Fret Zealot：`fretzealot.com`；小叶子：`xiaoyezi.com`
- 开源：`github.com/matthewcaren/web-score-following`（MIT，OTW）、`github.com/CoderLine/alphaTab`（MPL-2.0）、`github.com/Perlence/PyGuitarPro`（LGPL-3）、`github.com/spotify/basic-pitch` + `basic-pitch-ts`（Apache-2.0）、`github.com/seingreen/STRAdi`（Apache-2.0）、`github.com/cwitkowitz/guitar-transcription-continuous`（MIT）、`github.com/marl/GuitarSet`（MIT）、`github.com/marl/crepe`（MIT）、`github.com/CPJKU/madmom`、`github.com/flippy-fyp/flippy`（GPL-3）、`github.com/MTG/essentia.js`（AGPL-3）
