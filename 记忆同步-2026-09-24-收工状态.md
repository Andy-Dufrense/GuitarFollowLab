# 收工状态 · 跟弹（2026-09-24 夜）

> ## ⚠ 新窗口第一件事：**把所有文档读一遍再动手**
> 顺序：`AGENTS.md`（铁律）→ **本文件** → `记忆同步-2026-09-23-夜-收工状态.md` → `思路.md`
> → `思路点评-2026-09-21.md` → `调研-跟弹判定做法-2026-09-23.md` → `调研-智能跟弹-竞品与开源对标-2026-09-20.md`
> → `跟弹-复盘与重新设计.md` → `跟弹-思路整理-2026-09-21.md` → `HANDOFF.md` → `README.md`
> → 9-20 / 9-21 / 9-22 / 9-23 的收工状态。
> **不读完不许改代码**；今天有 8 次改动是因为"没复现就动手"被撤回的。

---

## 0. 今天最完美的状态

**版本 `0924-2320`**（页面上显示 `v0924-2320`）。代码**未提交 git**（HEAD 仍是 `bf5e178`）。

| 验收 | 数字 |
|---|---|
| `gt-notes` 双向（39 个真机拨弦） | **39/39** |
| 错音测试 `tl-wrongtest`（弹 F4、谱面要 A3，差 5 个半音） | **对 0** |
| 琶音原谱 `tl-chordarp`（C-Am-F-G T3231323 弹对） | 对 8 / 错 6（**未解决的靶子**） |
| `test-follow-page` | 4 项不通过（全是已知的跟节拍老断言） |
| `check-build` | ✅ 17 个文件版本号一致 |
| 快弹两段（`2弦1品快速` / `4弦3品快速`） | 起音层 9/9 全中、不漏 |

> ⚠ **上面这一行"琶音原谱 对 8/错 6"已作废** —— 那个靶子的谱面和录音不是同一个和弦进行。
> 修正后的数字与全部结论见下面 **§0.5**（本窗口追加）。

---

## 0.5 收工后追加（本窗口 · 傍晚，版本 `0924-1725`）

> **判定行为与 `0924-2320` 完全一致**：本窗口新加的东西**全是默认关闭的诊断开关**
> （`VC_JUDGE_READ` / `VC_READ_GATE` / `VC_READ_VETO` / `VC_JUDGE_RISE` / `VC_JUDGE_PREV`），
> 手机拿到的是**干净版本**。这一节改动了 §0 / §4 / §5 / §6 里几个数字，别按旧的用。

### 0.5.1 ⚠ 最大的发现：琶音靶子配错了进行（旧数字作废，别重复走）

量 6415 那段录音每 0.5s 一格的**低音**（75~165Hz 最强线）：
`A2 → F2 → C3 → G2` 循环 = **`Am-F-C-G`（就是"6415"）**；
而靶子用的 `frontend/data/chord_arp.json` 是 **`C-Am-F-G`** —— **谱面和录音不是同一个进行**。
（工具：`vc_gf/arp-bass.mjs`）

按录音真实进行重建靶子（`vc_gf/mk-tl-6415.py` → `tl-6415.json`；移调 `tl-6415-t4/5/6.json`）后重测：

| 靶子 | 旧（配错进行 → 作废） | **修正后（现用）** |
|---|---|---|
| 琶音原谱（用户弹对） | 对 8 / 错 6 | **对 30 / 错 3** |
| 移调 +4 | 对 4 | **对 3** |
| 移调 +5 | 对 9 | **对 3** |
| 移调 +6 | 对 1 | **对 2** |

⇒ §5 第 1 条"差 ±4/5/6 侥幸过"的**真实规模是 3/3/2**（不是 4/9/1）；原谱那条路其实很干净。

### 0.5.2 本窗口试过并撤回（别重复走）

| 试的 | 数字 | 为什么撤 |
|---|---|---|
| §6「起音即读数」（起音 ±40ms 窗 → 成串基频 → 音名判） | F4 正面 **16/16** ✅、谱面要 A3/C4/B3 **对 0** ✅，但 **±4/5/6 没修好**；hey_jude 24 音标定集**误杀 5** | 读数真机上不可用（6段标定 17/39、旋律 2/24、琶音 0/52）→ 按"误杀>1 就撤" |
| 读数 + "两个窗意见一致"可信度门 | 仍误杀 5/24 | 门分不开对错 |
| `fitMax` 按弦收紧（3~6 弦 → 200） | 会**误杀 3 个弹对的**（30 个里 fit=203/218/227） | 弹对/弹错区间重叠（188~227） |
| `rise` 抬头加权（engine 里现成） | 原谱 30/3 → **28/12**；+5 3 → 1 | 误杀 2 > 1 |
| `prevMidi` 剔除上一音余响 | 全档零效果 | 无收益 |
| **按弦隔离 + 延迟**（+90ms 对 +220ms） | 正确组"本音最好"只有 **21~22/55**（等到 220ms 一样）；候选余量门与对错**无相关**；抬头竞争只有 5~8/55 | **+130ms 延迟买不到这个能力** → 不建议加延迟 |

一句话机制：候选**窄**（只留 ±1/±2 品）→ 差 ±3~±7 时表里没有你实际弹的那个音 → **漏**；
候选**宽**（整根弦 25 品）→ 远端候选靠巧合赢，**正确组 60% 会被误杀**。两条边都量了。

### 0.5.3 下一步（三选一，等用户拍板）

1. **接受限制**：手机麦克风 + "和弦一直在响"的材料上，原谱 30/33 判对，±4/5/6 漏 3 处；
2. **换输入**：电吉他 DI 直录（最好六路拾音）——同一个测量问题从"很难"掉到"工程问题"；
3. **上学习式逐弦模型**（FretNet / TabCNN 那类，GuitarSet 训练，只用于离线复核）。

### 0.5.4 本窗口新增的离线工具（`C:\Users\Administrator\vc_gf\`）

`read-sweep.mjs`（窗/读数/可信度扫描）｜`arp-bass.mjs`（量每格低音 —— 就是它抓出进行不一致）｜
`arp-align.mjs`（按相位对齐再扫窗）｜`arp-cand.mjs`（候选比赛 + 余量）｜
`string-isolate.mjs`（按弦隔离，含单和弦对照）｜`string-rise.mjs`（抬头竞争）｜
`mk-tl-6415.py` + `tl-6415*.json`｜`tl-f4pos.json` / `tl-f4neg.json` / `T-f4pos.f32`

```bat
cd /d E:\GuitarFollowLab
set VC_TIMELINE=C:\Users\Administrator\vc_gf\tl-6415.json
node test\test-follow-real.mjs sound_data\f32\6415\6415慢速.f32      :: 修正靶子：对 30 / 错 3
node C:\Users\Administrator\vc_gf\string-isolate.mjs                 :: 按弦隔离 / 延迟那张表
node C:\Users\Administrator\vc_gf\string-rise.mjs                    :: 抬头竞争那张表
```

---

## 1. 今天保留的改动（都验过，按文件）

| 处 | 改动 | 治什么 | 依据数字 |
|---|---|---|---|
| `follow-score.js` 判定段 | **读数不再当硬闸门**（`detOk = true`）；「听到」那行只在读数与谱面一致时才显示它 | 1弦1品 弹对判错 | 离线 hey_jude "只因读数判错" 3 → 0；导出里 20 个非空读数 19 个是垃圾 |
| `follow-score.js` 候选 opts | 有弦品的格子 `maxOffset = 2`（候选只留 ±1/±2 品） | "约 F3/F2" 这类误导说法 | 1弦1品 不再被说成低八度 |
| `analysis.js` `mismatchOf` | **按弦限带**（`bandLo/bandHi`）：带外的峰不参与 O2P | 余响/闷响把本音罚到"没证据" | 24 音旋律标定集 对 7 → 对 14（⚠ 该标定集**后来作废**，见 §5） |
| `onset.js` `decideOnset` | **冷却期内有 2kHz 抬头就放行**（`coolOk`） | "那个速度下漏一个" | 导出里"冷却中"曾占 47%；快弹两段 9/9 |
| `judger.js` `JUDGE.octaveGuardCents` | 80 → **120** | 同一个音第一次错、第二次才过 | idx82 本音失配 199、领先 1.24 却被低八度守卫否掉 |
| `follow-score.js` 跳音分支 | 必须**双证据**（下一格失配 <190 且好 60） | 真拨弦被记成"漏了某某（跳过去了）" | 用户 10:27 导出 21.167s 那处 |
| `follow-score.js` judge opts | **响的音（≥0.10）本音必须领先邻居 ≥1.05**（轻音仍 0.90） | ±5 侥幸过（F4 连弹那 5 次） | 错音测试 对 5 → 对 0 |
| `follow-score.js` 跟节拍 | **逐音时间账**（用"这一下与上一拨的间隔"比谱面间隔）+ 总时间按**比例**（`CFG.tempoTotalTolPct = 0.15`） | "全弹快没有提示" | 用户口径：只要提示、不算不合格 |

---

## 2. 今天试过并**撤回**的（8 条，别重复走）

| 试的 | 结果 | 为什么失败 |
|---|---|---|
| 扩对手集 ±1..±7 | 差4/5/6 挡住，但 `gt-notes` 39→38、琶音 5/0→2/2 | 混音里远端邻居靠巧合赢过本音 |
| 两级领先线（远端要 1.25） | 差4/5/6 挡住，琶音 5/0→**0/1**、`gt-notes` 38/39 | 同上，更严=更误杀 |
| 补候选洞 + 远端"自己得拟合好"(fit<200/180) | 靶子挡住，`gt-notes` 38/39（收紧反而 37/39） | 多出的候选扰动 `candBest` → 跳音分支被带偏 |
| O2P 掩码（只罚"新加进来"的峰） | 错音测试 对0→**对1**、`follow-page` 4→5 | 掩码把"反驳错音的证据"一起拿掉 |
| 差分谱（40ms，判定时刻为准）当判定输入 | 琶音 对4→**对2** | 稀疏谱 + 旧阈值不匹配 |
| ±5 进对手集 | **零效果**（移调靶子一个没变） | 竞争者不是 ±5，而是"本音本来就得高分" |
| P2O 抬头守卫（窄化：电平≥0.10 且 3~6 弦） | 零效果；琶音 错6→错7 | 条件把靶子的情形（轻音、1~2 弦）排除了 |
| **以起音为中心的差分谱当判定输入** | 移调 +4 对4→**对8**、+6 对1→**对9** | 峰变少 → P2O/O2P 一起缩 → 失配整体下降 → "绝对线 250"更容易满足 → 错音过得更松 |

**总结论**：在**现在这扇被污染的判定窗**（起音后 90ms 往回 170ms，含上一根弦余响 + 拨弦闷响）上做任何门槛/对手/掩码 —— **要么无效，要么误杀**。这是"证据不干净"，不是阈值问题。

---

## 3. 今天的用户口径（原话，必须遵守）

1. **"不固定窗口看起音，而是在每一次起音的周围定窗口，在这个窗口内判定音准"** —— 窗要以**起音采样点**为中心。
2. **"上一根弦的余波，在这次起音是下降的，我们只计算起音的那条线"** —— 只留上升的那条链。
3. **"识别出音准之后和期望音判断，不在容忍度内就判错，不管是差了 1 品 2 品还是 12 品"**。
4. **"同一个音名就给过；C4 和 C4 过，C4 和 C3、C4 和 C#4 都不过"**。
5. 跟节拍：**抢拍/拖拍/整体快慢只提示，不算不合格**（这个项目以陪练为主）。
6. 读数**不可信时不许硬判**（退回原链路，防误杀）。

---

## 4. 今天建立的可复现靶子（最大产出，`C:\Users\Administrator\vc_gf\`）

| 靶子 | 怎么跑 | 期望 |
|---|---|---|
| **琶音原谱**（C-Am-F-G × T3231323，和弦在响）⚠ **配错进行，见 §0.5.1** | `python vc_gf/mk-tl-from-notes.py frontend/data/chord_arp.json vc_gf/tl-chordarp.json 3` → `VC_TIMELINE=vc_gf/tl-chordarp.json node test/test-follow-real.mjs sound_data/f32/6415/6415慢速.f32` | ~~现在 对 8/错 6~~ → **修正后 对 30/错 3** |
| **琶音移调 +4/+5/+6**（用户弹原谱、谱面移调 = 差 4~6 个半音的错音）⚠ **配错进行，见 §0.5.1** | `python vc_gf/mk-tl-transpose.py frontend/data/chord_arp.json vc_gf/tl-arp-t5.json 5 3` → 同上换 VC_TIMELINE | **必须 对 0**（~~现在 +4 对4 / +5 对9 / +6 对1~~ → **修正后 对 3/3/2**） |
| **错音必挡**（1弦1品 F4 连弹、谱面要 A3） | `tl-wrongtest.json` + `vc_gf/f4-user-0924.f32` | 对 0 ✅ |
| **单和弦琶音（无偏差循环标签）** | `tl-arp-all.json` + `sound_data/f32/C-T3231323.f32` | 对 4/错 1 |
| **快弹两段** | `tl-fast2/4.json` + `vc_gf/fast-2s1.f32` / `fast-4s3.f32` | 起音层不漏（9/9） |
| **差4/5/6（单音）** | `tl-off456.json` + `vc_gf/f4-user-0924.f32` | 对 0 ✅ |
| 杂音三段 | `vc_gf/voice-talk.f32` / `voice-cough.f32` / `neighbor.f32` | **必须一下都不算**（现在 0/1/3 ❌） |
| 工具 | `why-pass.py`、`step-check.py`、`tail-check.py`、`attack-audit.mjs`、`band-attack.mjs`、`read-eval.mjs`、`label-heyjude.mjs` | 导出/录音的逐条证据 |

⚠ **两个教训**：① 标定集的标签**不能靠对齐**（`hey_jude.f32` 那句"和谱面对不上"就是按序号配出来的假结论；按**时刻**配才对得上）；② 标定集的标签**不能用会自证的读数**（我用锚定读数验过 24/24，那是循环验证；只用"读数稳"的样本又是幸存者偏差）。今天因此作废了两把尺子。

---

## 5. 未解决的四个问题（都有靶子）

1. **差 ±4/5/6 个半音侥幸过**（用户实测；靶子：琶音移调 +4/+5/+6 ~~现在 对 4/9/1~~ → **修正靶子后是 对 3/3/2**，见 §0.5.1）
   机制：**候选表有洞**（`CANDIDATE_OFFSETS = [0,±1,±2,±5,±12]`，对手集只有 ±1/±2）→ 差 ±3/±4/±6/±7 时表里没有"你实际弹的那个音"，判定退化成"本音绝对分 < 250 就过"；而这些假判过**全发生在和弦在响时**（谱面要的音常常正好是已在响的和弦音）。
2. **琶音大面判错**（用户手机导出 `json/follow-log-arp-1790234787177.json`：80 起音 / **bad 62** / 步 4 被重弹 42 次；离线复现：6415慢速 对 8/错 6）
   机制：60/80 个音的**本音失配 ≥250（"没证据"）**，而锚定读数常常是对的（如 G3 −7.9 音分）→ **是分数不可用，不是音高测不准**。
3. **杂音三段不是"一下都不算"**（说话 0 / 咳嗽 1 / 邻居 3）；嫌疑是今天的"冷却期放行"，还没 A/B 定位。
4. **和弦页"一个 1弦空品过所有和弦"**：和弦判据只问**解释率**、不问"和弦里每个音是否都响着"；修法：借用双音/三音已有的"每个音各自谐波显著性 ≥2.0（缺哪个报哪个）"。

---

## 6. 下一步（第一步钉死，照用户思路，一步不改）

**把昨天那套"起音即读数"的思路做对**（用户原话见 §3）：

1. **窗以起音采样点为中心**：pre = [起音−40ms, 起音−10ms]，post = [起音+10ms, 起音+40ms]（`buf` 有 341ms，够反推，**不需要额外环形缓存**）；
2. `post − pre`（负的归零）→ "这一下新加进来的谱"；
3. 在这份谱上量**读数**：最响的、自带 2f/3f 成串、且不是更低那根谐波的基频；
4. **可信度门限**：不够可信 → 退回现有候选比较（**不硬判**，防误杀）；
5. 按用户口径判：读数与谱面音名不同（差 ≥2 个半音）→ **判错**；同名 → 过。

**验收（两面都要过，缺一不算）**：

| 靶子 | 目标 |
|---|---|
| `一直弹1弦1品` + 谱面要 **F4**（弹对） | 16/16 全过（现在没有这个靶子的时间轴，要先做 `tl-f4pos.json`） |
| `一直弹1弦1品` + 谱面要 A3/C4/B3 | 对 0 |
| 琶音移调 +4/+5/+6 ⚠ 用**修正靶子**（`tl-6415-t4/5/6.json`，见 §0.5.1） | 对 0（修正后现状 对 3/3/2） |
| 琶音原谱 ⚠ 用**修正靶子**（`tl-6415.json`） | 对 30/错 3 不许更差 |
| `gt-notes` / 错音测试 / `follow-page` | 39/39 / 对 0 / ≤4 |

---

## 7. 常用命令

```bat
cd /d E:\GuitarFollowLab
node test\gt-notes.mjs            :: 39/39（底线）
node test\test-follow-page.mjs    :: 4 条已知
node test\check-build.mjs         :: 版本号一致
node C:\Users\Administrator\vc_gf\bump-build.mjs 0925-XXXX   :: 改版本号（17 个文件）
```

---

## 8. 今天的教训（写进铁律）

1. **先复现，再改代码**。今天 8 次撤回全是因为"在没复现的地方改"；而真靶子一建出来（琶音移调），机制当天就定位了。
2. **不要用会自证的读数量标签**；**不要用"只收读数稳的样本"的标定集**（幸存者偏差）。
3. **A/B 只看数字**，一条改动不允许顺带改别处；误杀 >1 个就撤。
4. **误杀和侥幸过是同一枚硬币的两面** —— 任何"更严"的规则都必须同时在两面靶子上验。
5. **别在"只问一半"的判据上继续加补丁**：我们的判定只问"谱面音在不在"，它**天生抓不到远距离错音**；要抓远错音，就得真的**测音准**（用户 §3 的思路）。

---

## 9. 本窗口下半场（谱面 / 页面 / 修两个"谱面不对"的 bug）

> 这一节是 §0.5 之后接着做的，**最新状态看这里**。版本 **`v0924-2015`**。
> 判定行为仍然和 `0924-2320` 一致（本窗口新加的全是**默认关闭**的诊断开关）。

### 9.1 打通"自己写 .gp 谱子"（不需要 Guitar Pro）

仓库里 vendor 的 **alphaTab 1.8.4 自带 `Gp7Exporter` / `AlphaTexExporter`** —— 本地就能把谱子写成 GP7。

| 工具（都在 `C:\Users\Administrator\vc_gf\`） | 干什么 |
|---|---|
| `gp-from-json.mjs` | **时间轴 JSON → .gp**：支持同一时刻多音（双音/三音）、休止、和弦名（只写每小节第一拍）、变调夹；**写完自动回读校验**（音数 / 弦品 / 实际音高），不一致会报出来 |
| `mk-variants.mjs` | 从 `frontend/data/chord_arp.json` 派生练习版时间轴 |
| `tune-probe.mjs` | 打印谱面的调弦数组 + 每个音的 string / fret / realValue（查"画反了"靠它） |
| `aligncheck.py` | 真 Chromium 里用**页面自己的 alphaTab + collectScoreSlots** 把"谱面格"和"时间轴"逐条对号（0 处对不上才算过） |
| `songcheck.py` | 真 Chromium 里切曲目 + 点「跟弹」（假麦克风），读对齐行 / 自检行 / pageerror / 4xx |
| `shot.py` / `orientcheck.py` | 截图 / 方向检查 |

### 9.2 谱子清单（都已提交）

| 谱子 | 内容 |
|---|---|
| `frontend/data/chord_arp.gp` | 原谱：4 小节 C-Am-F-G × T3231323（每小节 8 个八分音符 / 76BPM / 标准调弦 / 带和弦名）。**和判定清单 `chord_arp.json` 同一份数据** → 谱面 32 格 = 判定 32 个音 |
| `.gp/practice/chord_arp_slowrepeat.gp` | 慢速重复：4 个和弦 ×4 遍，每遍之间空**一整小节**（共 8 小节） |
| `.gp/practice/chord_arp_noT.gp` | 去掉 T，只练 3/2/1 弦（第 1 拍是休止） |
| `.gp/practice/chord_arp_T3.gp` | 只练 T 与 3 弦（T 3 T 3，四分音符） |
| `.gp/practice/chord_arp_dyad.gp` | 双音版：T / 3弦 / 〔1弦+2弦〕 / 3弦（和"1645-T 3 12双音 3"录音同型） |
| `.gp/practice/chord_arp_triad.gp` | 三音版：T / 3弦 / 〔1+2+3弦〕 / 3弦（和"1645-T 123（三音）"同型） |
| `.gp/practice/chord_arp_capo2.gp` | 变调夹 2 品（指法不变、音高 +2） |
| `.gp/practice/chord_arp_60bpm.gp` | 60 BPM 慢练 |

页面入口：**曲目下拉新增「C–Am–F–G · T3231323（真实谱面）」**（`SCORES` 表 + `index.html` 各加一行）。
谱面/时间轴地址都带 `?v=${BUILD}` —— 改谱子以后手机不会再拿缓存里的旧谱面。

### 9.3 今天修掉的两个真 bug（都表现成"谱面不对"）

1. **对号全报"对不上"**：`frontend/js/app/cursor.js` 读"品"只认 `note.value`，实测 **GP7 和 GP3 导入都写在 `note.fret`** 上 → 全部 fret=null。
   改法 `fret: (note.fret != null ? note.fret : note.value)`。依据：修前 `hey_jude.gp3` 118 处全对不上，修后 118=118 ✓。
2. **六线谱上下镜像（用户实测："谱子画反了"）**：两条约定都反了 ——
   - 调弦数组必须是 **1弦→6弦**：`[64,59,55,50,45,40]`（对照真谱 `.gp/the-beatles-hey_jude.gp3`）；
   - 内部 `note.string` 是**从最粗的 6 弦数起**（真谱里「2弦1品」= `string=5`），而我们的时间轴是人的数法 → 生成时 `note.string = 7 - 时间轴弦号`。
   两条一起反会**互相抵消掉音高**（所以自动回读校验没发现），但位置全镜像、调弦标记显示成 `E A D G B E`。

**以后写谱的规矩**：写完必须跑这三条，都过了才算接好（对号 0 处对不上、无 pageerror、无 4xx）：
```bat
node C:\Users\Administrator\vc_gf\gp-from-json.mjs <时间轴.json> <输出.gp>
E:\Python\python.exe -X utf8 C:\Users\Administrator\vc_gf\aligncheck.py "http://localhost:1209/" "./data/xxx.gp" "./data/xxx.json"
E:\Python\python.exe -X utf8 C:\Users\Administrator\vc_gf\songcheck.py "http://localhost:1209/" <曲目 value>
```

### 9.4 判定侧结论（没变，别再重复走）

- 单音：`gt-notes` 39/39；1弦1品 F4 连弹 16/16；错音（要 A3 弹 F4）对 0。
- 琶音（**修正靶子** `tl-6415*.json`）：原谱 对 30/错 3；移调 +4/+5/+6 = 对 3/3/2。（旧靶子的 8/6、4/9/1 作废）
- **按弦隔离在 +90ms 与 +220ms 都分不开**（正确组只有 21~22/55 能让谱面音当赢家）→ **不加延迟**。
- **双音/三音现在没有判别力**：茉莉花三音（5弦9品＋4弦11品＋3弦11品）正确谱面 对1/错1；"少写一个音"和"多写一个音"**结果一模一样**（都是"混进了多余的声音"）→ 那两版 .gp 只当练习用，判定别当真。
- 要"逐音逐品"只有三条路：**换输入**（电吉他 DI / 六路拾音）、**上模型**（先跑 basic-pitch 量单麦克风域差）、或**真正的按弦能量隔离**（工程量最大）。

### 9.5 下周开工第一件事（按顺序）

1. **手机上看新谱面**（版本 `v0924-2015`）：选「C–Am–F–G · T3231323（真实谱面）」→ T 应在下面几行（C/Am 的 T 在 5 弦那行、F/G 的 T 在最下面的 6 弦线）；点「跟弹」后对齐那格应为 **✓ 谱面32=判定32**。
2. 定方向（用户拍板）：接受限制 / 换输入 / 上模型。
3. 收尾小项：`backend/engine/cursor.js`、`backend/engine/follow-score.js` 两个旧副本（没被引用、没提交）要不要删；**`apply_patch` 工具今天坏了**（写文件报 sandbox 错），本窗口改用"确定性替换脚本"（`%TEMP%\vc_patcher*.py`：UTF-8 无 BOM、命中必须唯一）+ `node --check` + `git diff` 复核 —— 下次开工先试一下它修没修好。
