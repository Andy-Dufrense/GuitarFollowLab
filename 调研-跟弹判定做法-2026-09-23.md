# 调研：别人的"判定音准 / 判这一下对不对"到底怎么做

> 写于 2026-09-23。**只做调研与整理，不动代码**（用户明确要求）。
> 架构与竞品扫描见 `调研-智能跟弹-竞品与开源对标-2026-09-20.md`；
> 这一份只回答一个问题：**"这一下拨的，是不是谱面要的那个音"——别人怎么做的、我们哪里做错了。**

---

## 0. 三句话结论

1. **所有靠谱的实现都把"起音"和"音高/音符"分成两路**：起音分支只回答"这里有没有一次拨弦"
   （瞬态、能量、相对电平），音高分支在**起音之后的稳定段窗**里量。我们今天的 bug 就是把两路混在一起。
2. **判定用的谱窗必须只属于这一下**。包含上一根弦余响的长窗，会把"上一根的音"当成"这一下的证据"——
   这正是"我弹 F4、期待 A3 却判对""1 弦 1 品一路畅通"的机制。
3. **音分判定（±50 / ±80 / ±100）只有在"拿到可信基频"时才成立**。拿不到可靠峰时，
   estimator 仍会吐一个接近 0 的音分数 → `|cents| ≤ 阈值` 恒成立 → **什么都判对**。
   正确的做法是：**拿不到可信峰就报"没听清"，绝不报 0 音分。**

---

## 1. 学术上被反复验证的两路结构（Onsets and Frames 一脉）

| 工作 | 做法 | 对我们的意义 |
|---|---|---|
| **Onsets and Frames**（Hawthorne et al., 2018, Magenta） | 两个分支：onset 分支（帧级"这里有没有起音"）+ frame 分支（帧级音高），后接 note tracking | 我们 9-21 的 `思路点评` 已经引过；**起音不借音高**是它的核心 |
| **Basic Pitch**（Spotify, 2022, Apache-2.0） | onset + frame 两路、支持 polyphony、可跑浏览器（TF.js） | 复音场景的现成参考实现，可离线对标 |
| **Deep Salience**（Bittner et al., 2017） | 学"每个音高上的显著性"（salience），再做 F0 决策 | **多音（双音/三音）判定应该看 salience**，而不是拿单音尺子逐个判 |
| **PLCA / NMF**（Benetos 等） | 把频谱分解成"每个音高的谐波模板之和" + 残差 | **"解释率 + 每个音的存在性"就是我们该用的多音判据**（和弦那条 `chordOutsiders` 已经是它的简化版） |
| **TabCNN**（Wiggins & Kim, 2019）/ **FretNet** | 输出 **6 弦 × 时间** 的矩阵（逐弦概率） | 用户一直想要的"逐弦判断"，学术界正是这么做的；代价是要训练数据（GuitarSet 等） |
| **Antescofo（IRCAM）/ online DTW / OTW**（web-score-following, WAC 2024） | 谱面跟随：在线对齐 + 插入/删除代价 | 治"漏一个就整段错位"的正解（我们 9-21 的点评也点过名） |

---

## 2. 产品侧：别人怎么做"判对判错"

| 产品 | 输入 | 判定方式 | 关键事实 |
|---|---|---|---|
| **Rocksmith**（育碧） | **专用线接电吉他（DI）**，不用麦克风 | 每个音的 onset + pitch 与谱面比，命中/未命中 | **它从根上绕开了"别人在旁边弹/环境音"**——不是算法强，是输入干净 |
| **Yousician** | 麦克风/耳机麦克风 | onset + FFT 峰 + 音符判定，**命中/未命中两档**、时间窗宽松（几百 ms） | 明确让用户**戴耳机**、安静环境；宁松不严（体验优先） |
| **SmartMusic**（MakeMusic） | 麦克风（单声部乐器/人声教学） | 单声部基频检测 + **±50 音分**容差，"音不对/音不准/漏"分开报 | **音分容差在学术界/工业界的默认就是 ±50**；它只在拿到可信基频时才判音分 |
| **Soundslice / Songsterr** | 不做实时判定 | 只做播放 + 光标 | 说明"光标/谱面跟随"和"判定"是两件事，可分开做 |
| **各类跟弹 App 的普遍取舍** | — | 时间窗宽松（±100~250ms）、音高容差 ±50~100 音分、**复音只判"和弦音集合"** | 没有人用"锚在期望音上的读数"当门槛 |

**一句话**：工业界要么用 DI 把环境问题消掉，要么用"宽松窗口 + 命中/未命中 + 明确提示戴耳机"来降级体验；**没有人把不可信的读数当等号用**。

---

## 3. 我们踩过的坑 ↔ 别人怎么做（逐条对照）

| 我们做过的（错的） | 后果（今天实测） | 别人的做法 |
|---|---|---|
| 判定用 **170ms 长窗**，里面含上一根的余响 | 弹 F4（期待 A3）判过；1 弦最畅通；漏了之后后面还算对 | **只用"起音之后"的窗**（onset-aligned），或起音前后**短窗相减**（差分谱） |
| 用**锚在期望音上**的读数当放行门槛 | 半音错也能自证；显示"听到 G#3/A#3"永远只差半音 | 要么**不看答案**地量（差分谱 + 可靠度），要么**只用相对比较**（期望音 vs ±1/±2 邻居） |
| 给 1 弦/同音加**特例**（2kHz 抬头、谐波数、门限倍率） | 敲桌子判对 / 分解和弦丢音 / 邻座琴声进来 | **特例不进判据**；要区分就用**结构**（两路 + 差分谱 + salience） |
| 把**音分规则**接到不可靠的测量上 | `|cents| ≤ 80` 恒成立 → 什么都判对 | **可信度门禁**：拿不到可靠基频 → 报"没听清"，不给音分数 |
| 起音层"像不像琴声"用 **YIN clarity** 判 | 叠音（分解和弦、双音）必失效 → 真音被吞 | 起音分支用**瞬态/能量**；"像一根弦"用**谐波求和/salience**，且**只用来决定要不要消耗音符槽** |
| 丢起音（丢弃=不判不报） | 真正需要那一格时"怎么弹都过不去" | 丢掉必须**有替代反馈**（Yousician 报 miss；SmartMusic 报"漏"） |

---

## 4. 给我们的正确思路（据上面这些，落成可执行的几条）

1. **判定必须在"这一下"的谱上做**：用 `onsetDiffSpec`（起音前后短窗相减）或"起音后 30~170ms"的窗，
   **绝不能用含上一根余响的 170ms 长窗**。这是今天最大的那条 bug。
2. **音高判定走"相对比较"**：期望音 + ±1/±2 邻居，谁最像谁赢（已有），但**要在第 1 条的谱上做**。
3. **音分判定加可信度门禁**：只有 `score/nHarm/峰锐度` 达标时才用 `|cents| ≤ 容忍度`；
   不达标 → **"没听清"**（不计对、不计错，提示重弹），绝不吐 0 音分。
   容忍度沿用用户的 **±50 → ±80**（音准差一点能过、差到 100 音分即半音就错）。
4. **多音（双音/三音）= 存在性 + 解释率**：每个期望音各自要有谐波显著性（salience/harmonicity），
   再要求没有解释不了的多余能量；**缺哪个报哪个**。（= PLCA/NMF 的简化版；想要"逐弦"就上 TabCNN 那类模型。）
5. **起音分支只管"有没有拨弦"**：瞬态 + 相对电平 + 宽带证据；**不看音高**。
   "像一根弦"那一关放到"要不要消耗音符槽"那一步，判据用**谐波求和**，不用 YIN。
6. **环境干扰是产品取舍**：Rocksmith 用线、Yousician 让戴耳机。我们靠"电平门限 + 相对最近演奏电平"分档，
   并且明确：**邻座有人弹琴，单麦克风无解**（这一点要写进用户说明，不再靠算法硬扛）。
7. **漏一个就不再整段错位**：上"位置跟踪"（online DTW / HMM，带插入删除代价），
   这是学术界与 IRCAM 那套的标准解法。

---

## 5. 明确"以后不要再做"的清单（今天的教训）

- 不再把**任何**"锚在期望音上"的读数当放行门槛。
- 不再给某个音高 / 某根弦加**特例阈值**（1 弦那两次都是这么坏的）。
- 不再在**没有离线验证**的情况下动起音层与判定主路径。
- 不再把**不可靠的测量**接到"音分判定"上（拿不到可信峰就报"没听清"）。
- 不再用"丢掉起音"来挡杂音（丢掉会吞真音，且用户看不到原因）。
- 阈值只小步动（一次一个量，0.05~0.5 粒度），每档给"真音进来多少 / 杂音进来多少"两组数字。

---

## 6. 信息来源与说明

- 学术与开源：Onsets and Frames（Magenta, 2018）、Basic Pitch（Spotify, Apache-2.0）、
  Deep Salience（Bittner 2017）、PLCA/NMF（Benetos 等）、TabCNN（Wiggins & Kim, ISMIR 2019）、
  FretNet、Antescofo（IRCAM）、web-score-following（MIT, WAC 2024）。
- 产品：Rocksmith（专用线）、Yousician（麦克风 + 宽松窗口 + 命中/未命中）、SmartMusic（±50 音分 + 漏/错分报）。
- 本项目内部：`调研-智能跟弹-竞品与开源对标-2026-09-20.md`、`思路点评-2026-09-21.md`、
  `跟弹策略问题清单.md`、`记忆同步-2026-09-2*.md`。
- ⚠ 说明：本机没有外网，以上来自既有知识库与项目内部文档，**不是今天现查的网页**；
  若要写进对外材料，建议再核对一次各产品的当前版本行为与许可证。
