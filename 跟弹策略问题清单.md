# 跟弹：需要拍板的策略问题清单

> 2026-09-20。这不是技术文档，是**需要先定下来的问题单**。
> 每条写三件事：为什么必须定 → 可选项 → 我的倾向。标 🔴 的是"不定就没法往下做"。

---

## 0. 先把实测的能力边界摆出来

数字来自 `test/test-detect.mjs`，讨论以它为准：

| 场景 | 现状 |
|---|---|
| 单独弹一个音（含手机麦克风频响） | ✅ 准 |
| 250ms 一个音（120BPM 八分音符）连着弹 | ✅ 整段过 |
| 和弦余响里弹低音弦 | ✅ 已修（原来判高一个八度） |
| 完全不消音，250ms 以上 | ✅ 过 |
| **200ms 一个音连着弹** | ❌ 会"听到的总是前一个音" |
| **150ms 一个音** | ❌ 同上 |

200ms 过不去**不是参数没调好**：采集窗 341ms 是分辨低音弦半音所必需的，
而音符只隔 200ms，窗里三分之二装的是上一个音。靠"逐频点本底差分"减余响，
但**重叠的谐波减不开**（G3 和 E4 的某些谐波落在同一频点，减掉就把新音也减了）。
两条捷径（起音锚定短窗、起音前整窗差分）都试过并失败，原因已写进代码注释。
真正的出路是第 3 条。

---

## 1. 🔴 弹错了：停还是不停

这是分水岭，其他决策都依赖它。

| 方案 | 行为 | 代价 |
|---|---|---|
| A. 过关式（现在只有这个） | 弹错停在原地重弹，弹对才往下走 | 节奏永远是断的，练不了"流畅" |
| B. 跟弹式（不停） | 按谱面时间轴一直走，只把错标出来 | 需要"我现在认为你弹到哪了"的时间模型 |
| C. 两种都要，用户切换 | 慢练用 A，原速用 B | 两套判定策略并存（架构已经分开了） |

**我的倾向：C。** 用户要的就是这两种模式，而且判定口径本来就不同：
慢练问"这个音对不对"，跟弹问"你弹到哪了、错了几处"。
**要你定**：如果只能先做一个，先做哪个？

---

## 2. 🔴 错了怎么提示（前提是不打断）

| 提示方式 | 说明 | 风险 |
|---|---|---|
| 谱面/光标上标红 | 错的那个音变色，光标继续走 | 用户可能没看见 |
| 角落计数（第 3 处） | 只显示累计和最近一次 | 反馈弱 |
| 声音提示 | 短"嘀"一声 | 会被麦克风收进去，反而误判 |
| 弹完再报 | 这一遍不打断，结束后汇总 | 闭环慢，但最不干扰 |

**要你定**：
1. 错了是当场标一下，还是这一遍结束再说？（倾向：当场标 + 结束汇总）
2. 同一个地方连错 3 次，要停下来提示，还是继续走？
3. 提示里要不要说"你弹的是哪个音"？（判定已经能说出听到的是哪个音）

---

## 3. 🔴 速度上限：200ms 以下怎么走

| 方案 | 原理 | 能到多快 | 代价 |
|---|---|---|---|
| A. 开集识别（现在） | 不管谱面，先问"这是什么音" | ≥250ms/音 | 到顶了 |
| B. **时间片验证**（推荐） | 跟弹时**已知该弹哪个音**，不再"识别"，只在自己的时间片里验证它出现没有 | 150~200ms | 需要时间轴（已有雏形）；慢练用不上 |
| C. 分带跟踪 | 六根弦各自频带里分别跟踪 | ~100ms | 低音弦频带重叠，工程量大 |
| D. 神经网络复音转写 | Basic Pitch 之类 | 理论最快 | 手机算力/延迟/体积；已知答案却去识别，浪费 |

**我的倾向：B**（它同时是第 1 条选 B/C 的前提），C 留作下一步。
**要你定**：目标曲目里最快的音间隔是多少？（Hey Jude 实测最短 197ms —— 这个数决定了要不要马上做 B）

---

## 4. 速度怎么调

| 问题 | 选项 | 我的倾向 |
|---|---|---|
| 谁定速度 | 用户选 / 系统按上次表现推荐 / 系统推荐+可改 | 第三个 |
| 卡住时 | 自动降速 / 提示用户降 / 不管 | 连着两次失败才自动降（防抖） |
| 变速粒度 | 每次 ±5% / 固定档位 / 目标速度的百分比 | 固定档位，用户看得懂 |
| 升速条件 | 一遍没错就升 / 连续两遍 / 手动 | 连续两遍 |
| 允不允许"等用户" | 原速模式不允许；慢练模式必须允许 | 这是两种模式的分界 |

**要你定**：默认起始速度取谱面速度的百分之多少？（倾向 70%）

---

## 5. 时间层：跟弹怎么知道"走到哪了"

| 方案 | 说明 | 我们的条件 |
|---|---|---|
| 在线时间规整 OTW / 在线 DTW | 学术界标准做法，有 MIT 许可的开源实现可参考 | 现有的观测分数直接当代价矩阵，最省事 |
| 隐马尔可夫 / 粒子滤波 | 能建模"速度连续变化"，更稳 | 复杂，参数多 |
| 时间轴 + 容错窗口 | "现在该第 n 个音，允许 ±1 个音" | 最简单，但速度变化和跳音鲁棒性差 |

**我的倾向**：先做 OTW（一天量级），不够再上粒子滤波。
**要你定**：允许用户中途停下来吗？停下来算"暂停时间轴"还是"继续走、停顿算错"？

---

## 6. 谱面与时间轴要支持到什么程度

.gp 解析已经通了（`backend/tools/gp_timeline.py`，Hey Jude 实测：76BPM / 24 小节 /
旋律轨 118 个音 / 伴奏轨 622 个音）。下面这些直接影响能不能用：

1. **反复记号**（repeat、D.S.、Coda）：跟弹必须按**演奏顺序**展开，现在是线性顺序。
2. **多轨**：跟弹用哪一轨？（Hey Jude 有旋律轨和钢琴轨）用户自己选还是我们指定？
3. **变调夹 / 特殊调弦**：谱面指法与实际音高不同，判定要跟着变（算法已支持 capo）。
4. **技巧**：滑音/击勾弦/推弦/泛音在谱面上有标记，判定口径要单独定。
5. **扫弦/和弦**：判定是"每个音都对"还是"整组指纹像"？（现在是后者，宽松得多）

**要你定**：第一批支持的曲子长什么样？（纯单音旋律 / 分解和弦 / 扫弦伴奏）

---

## 7. 评分口径

跟弹过程中能积累的原始数据：每个音的对错、时间偏差（ms）、音准偏差（音分）、漏弹、多弹。

**要你定**：
1. 给不给分数？（倾向：先只给"错了几处、哪几处"，判定稳了再加分数 —— 判定不稳时给分会放大不信任）
2. 时间偏差算多严？（±40ms / ±80ms / 只判有没有）
3. 跟弹结果要不要进 VirtuCoach 那份 AI 反馈报告？

---

## 8. 设备与环境（纯手机麦克风）

1. **延迟校准**：跟弹要放伴奏，而手机外放会被麦克风收进去。做不做"敲一下"的校准？
2. **耳机**：要不要强制戴？（不戴的话伴奏会被当成你弹的音）
3. **蓝牙耳机**：延迟常 150ms+，比判定窗口还大 —— 要不要直接不建议？
4. **持续噪声**：风扇/说话的实测还没做过（现在只有自适应噪声地板，合成验证过）。

---

## 9. 数据闭环

1. 判定失败的样本要不要回收，留给团队分析？
2. **真机录音怎么拿**（最缺）：找几个人各录几段——不消音弹 T3231323、六根空弦、
   一段快到 200ms 间隔的。到目前为止所有验证都还是**合成信号**。
3. 隐私：录音要不要上传？（本机分析不必上传；要上传需单独说明）

---

## 10. 我建议的下一步

1. 你给 3~5 段真机录音 → 我把合成验证换成真实验证。
2. 定第 1 条（停不停）和第 3 条（速度上限路线）→ 定了"原速跟弹"才能开工。
3. 我把 .gp 时间轴接上（含反复展开），先用 Hey Jude 的旋律轨做"谱面 → 光标"这一半，不依赖音频判定。
