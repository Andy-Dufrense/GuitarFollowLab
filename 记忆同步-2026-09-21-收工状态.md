# 收工状态 · 跟弹（2026-09-21 晚，明天从这里接）

> 上一个会话的详细过程在 `记忆同步-2026-09-21-跟弹.md`（第 10、11 节是本轮加的）。
> 这一份只写"现在是什么状态、明天第一件事、别再踩的坑"。

**当前版本号：`0922-2130`**（前缀沿用了上一版的写法，就是这一轮的那一版；下一轮改代码时换个新号）。
代码停留在：`node test/test-follow-page.mjs` 全部通过、`node test/test-follow-real.mjs` 对 29/错 6。

---

## 0. 明天第一件事：手机上验光标（等用户回话）

用户上一轮报的是「**C4+18音分。光标也没修好啊**」——读数那句是「听到」那一栏（见下面 §3），
光标那一条是**真 bug**，这一轮修了，但**还没在手机上验过**。要问他的就是三件事：

1. 标题 /「导出记录」旁边是不是 **v0922-2130**；
2. 旁边那格写的是 **「对齐 ✓ 谱面118=判定118」** 还是 **「对齐 ⚠ …」**（⚠ 就把数字念出来）；
3. 光盘（光标）是不是**从第一小节的 2弦1品**开始、**一格一格**往前走、第二行还乱不乱。

顺手要确认的两条（都是老问题，这一轮可能连带修好）：
延音还要不要弹两遍；弹对的是不是还判错。

---

## 1. 这一轮改了什么（只动了**光标层**）

### 1.1 根因：判定清单和光标不是同一份，差一个音（已修，有证据）

- 判定清单 = 时间轴 `frontend/data/hey_jude.json`（**118** 个音）；
- 光标位置 = alphaTab 解析出来的谱面拍点（`collectScoreSlots`，导出里是 **117**）。

差的那一个来自延音过滤条件。浏览器里真跑的那份构建（`frontend/vendor/alphaTab.min.js` 原文）：

```js
tieOrigin=null; tieDestination=null; isTieDestination=false;
get isTieOrigin(){ return null !== this.tieDestination }
```

`tieDestination` 挂在**延音起点**身上，只有 `isTieDestination` 才是"我接在别人后面、这拍不用弹"。
上一版写成 `isTieDestination || tieDestination` → **起点和接续两拍一起丢**。

用 PyGuitarPro 直接数这份谱（新增 `test/probe-gp3-beats.py`）：

```
轨 0「Voice」：119 个有音符的拍，1 个是延音接续 → 要弹 118 个音
第 1 个音：小节1 拍1 tick960 2弦1品(C4)
```

119 − 2 = **117**，和导出记录里的数字对上。修法：`isTieDest = (n) => !!(n && n.isTieDestination)`。

### 1.2 改法：光标和判定合成**同一条清单**

- 删掉"按时间就近配"的整套逻辑（`alignNotesToBeats`），换成纯函数
  `mapSequenceToSlots(notes, beats)`（`frontend/js/follow-score.js`）：
  第 i 个要弹的音 ↔ 第 i 个谱面位置，**单调**，而且每次加载**逐条验音高 + 品**
  （用 `note.realValue`，跟弦号怎么编号无关）；
- 对得上 → 页面 `#align` 写「对齐 ✓ 谱面118=判定118」；对不上 → 「对齐 ⚠ …」写明差多少，
  退回"按品+音高只往后找"，**绝不再悄悄错开一位**；
- 整条路**不再依赖 `beat.absoluteStart`**（手机上它经常是 undefined，以前按时间配会全体
  塌到第一拍 → "光标停在第一个不动"）；
- 顺手：开始跟弹时**立刻**把光标画在第一个音上（`startMic` 里 `highlightCurrent()`）；
  跟弹进行中**点谱面不再偷改起点**（要先点「停止」）；点谱面从头练某一段重新可用。

### 1.3 验在哪儿

`test/test-follow-page.mjs`：§13 复现 117 的成因；§11 映射（一一对应 / 少一格要标 ⚠）；
§13b 整条链路走一遍（假谱面含延音接续 → 光标表必须是 `[第1拍, 第2拍, 第4拍]`）。
离线工具：`test/probe-gp3-beats.py`（谱面基准）、`test/probe-timelines.mjs`（两份时间轴逐条比）。

---

## 2. 这一轮**试了但没上线**的：差分谱判定（别重复劳动）

想法：起音那一刻 `buf` 切两半（各 170ms）→ 各做 8192 点谱 → `diffMags(post, pre)` →
在 ±250 音分内量音高 → `|cents| ≤ 75` 判过。想把"上一个音还在响"减掉，一次解决
弹错判对 / 快音第二下 / 1弦1品轻音。

| 判据 | 合成（弹错音矩阵） | **真机录音**（30 秒、全弹对） |
|---|---|---|
| 老：起音快照 ±80 | 8 种错法**全判对**（已知缺口） | **对 29 / 错 6** |
| 新：差分谱 ±250 | 8 种错法**全判错**（缺口补上） | **对 10 / 错 25** ❌ |

真机上是明显退步（读数变成"约 B3 要 C4"这种差一点点的假读数）→ **没有上线**。
代码里留着开关和记录字段：`JUDGE_DIFF` 默认 false，导出记录里多了 `lit` / `dCents` / `dHz`；
复现：`VC_JUDGE_DIFF=1 node test/test-follow-real.mjs sound_data/f32/hey_jude.f32`。

**第四次被合成信号骗的前车之鉴：合成只有一根弦在响，一切"分不开"的判据在它上面都好看。**

---

## 3. 「听到 C4 +18音分」怎么读

那是界面上「听到」那一栏（`$('heard')`），值 = `estimateF0ByPeaks(判定窗, …, exp.midi, {})`。
**它是在"谱面这个音"附近量的**，所以它显示 `C4 +18音分` 只能说明"这一下的谐波落在 C4 位置上"，
证明不了"你弹的就是 C4"（弹成隔壁音它也可能在附近捡到东西）。要回答"你弹成了别的音"，
必须有**不看答案的测量** —— 就是 §2 那条，还没做成。

---

## 4. 还没解决的（下一轮的清单，按优先级）

1. **弹错判对 / 弹对的判错**（测量层，最关键）：真机录音上"全弹对"还有 6 个判错（对 29/错 6），
   反过来"随便弹"也有不少判对。要做的是不看答案的音高测量，四条具体试法：
   - 差分谱改成**只取起音后 40~60ms 的短窗**（用多谐波联合投票补频率分辨率）；
   - 或者两半先做**包络归一化**再减（`analysis.js` 的 `removeDecayBias` 就是干这个的）；
   - 同一根弦重复音会**自相消**（减完基频掉进噪声）→ 单独一条分支；
   - 验收：真机 30 秒录音 **≥ 29/35 且不把对的判错** + 合成错音矩阵 **8/8 判错**，两条都要过。
2. **快音的两个音并成一个**（起音层）：用户那 6 段"拨两次/两根弦飞快换"的录音
   （`sound_data/f32/`）就是为此录的，要用它们做验收。
3. **1 弦 1 品偶尔收不到**（起音层）：轻、高，被低音弦盖住；同 `sound_data/f32/2弦1品-1弦0品.f32`。
4. 谱面记法：第 2 小节那处 `hammer=True` 但实际要弹两个音（用户已确认是谱子的问题），
   「跳过这个音」按钮就是为这种地方准备的；`h` ≠ 延音（用户之前误以为要弹两遍）。

---

## 5. 常跑的命令

```bat
cd /d E:\GuitarFollowLab
node test\test-follow-page.mjs                                   :: 合成回归（含光标 §11/§13/§13b）
node test\test-follow-real.mjs sound_data\f32\hey_jude.f32        :: 用户真机录音，期望 对29/错6
node test\test-product-page.mjs & node test\metro.mjs & node test\test-live.mjs & node test\test-detect.mjs & node test\test-outsiders.mjs & node test\test-dsp.mjs
set PYTHONPATH=E:\VirtuCoach-Lib
E:\Python\python.exe -X utf8 test\probe-gp3-beats.py .gp\the-beatles-hey_jude.gp3 --track 0
node test\probe-timelines.mjs
```

`test\test-bughunt.mjs` 有 1 项老失败（"击弦落点 F3"），在 `main.js` 那条调试图链路上，
和跟弹页无关，一直没管。

## 6. 铁律（吃过亏的）

- **不在手机上试错**：每次先跑合成回归 + 用真机录音离线量，数字拿出来再上手机；
- **合成不可信**（已经骗了四次）：只有一根弦在响、电平和真机差一个数量级；
- 改代码**必须改 `BUILD`**（用户在手机上看的就是这个号）；
- **每轮只改一层**，并在页面上/导出记录里留下可看的结论（`#align` 就是这么来的）；
- 用户要的是「对就是对、错就是错」，两档，不要"测不准"当缓冲。
