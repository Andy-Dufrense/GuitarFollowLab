# 收工状态 · 跟弹（2026-09-22 晚，明天从这里接）

> 这一份只写"现在是什么状态、明天第一件事、别再踩的坑"。
> 细节过程和标定数据都在下面，够明天直接接着干。

**当前版本号：`0922-1935`**（页面上会显示 `v0922-1935`）。
**代码还没提交**（工作区有一整天的改动，见 §7），HEAD 还是 `bf5e178`。
回归状态：`test-follow-page` / `test-detect` / `test-live` / `test-product-page` /
`metro` / `test-dsp` / `test-outsiders` 全过；`gt-notes` **39/39**；
`test-bughunt` 1 项老失败（击弦落点 F3，改动前就失败）。

---

## 0. 明天第一件事：等用户在手机上验"跳音"

今天最后修的是**跳音**（下面 §1.3）。用户手里那版就是 `0922-1935`，
要问他的就一句话：

1. 右上角版本号是不是 **v0922-1935**；
2. 走一遍 Hey Jude 和 6415，**还有没有"弹一下光标往前走好几个音"**；
3. 如果还有，让他点「导出记录」，我去看 `json/follow-log-*.json` 里
   每一次起音的 `hfBand` 字段（新加的）和 `result`。

确认好了 → `git commit` + `git push origin main`（提交信息建议：
「起音：和弦里用频带抬头认起音（含基频锁，修跳音）；技巧：击弦/勾弦/滑音一次起音两个音；
防缓存：模块地址带版本号 + 启动自检」）。

---

## 1. 起音层（今天的主线）

### 1.1 要解决的问题

用户口径：「和弦/分解和弦里有些音要弹两次才过」——他没弹错，是系统没听见。

### 1.2 根因（真机量出来的）

起音判据原来要求"**整段混音的电平跳 1.4 倍**"。分解和弦里前一根弦还在响，
再添一根弦对总音量几乎没影响：

```
6415慢速 漏掉的每一次拨弦：总电平 0.90~1.11 倍（要求 1.4）
                          同一时刻 1.5~4kHz 涨 2~4.6 倍
```

判据量错了对象——量的是混音音量，证据在频带里。

### 1.3 改法（`backend/engine/analysis.js` + `backend/engine/onset.js`）

新增两条测量（都是"比自己 2 帧前 = 32ms"，窗 43ms）：

- `hfBandRiseOf()`：2kHz+ 频带抬头率
- `lowBandRiseOf()`：150~600Hz（**基频区**）抬头率

起音层新增判据（只在"总电平过门限、形状也变了"的基础上当额外证据用）：

```
hfBandOk = hfBandRise > 2.5 且 lowBandRise > 1.2 且 lv > lagged × 0.85
```

`hfBandOk` 可以替代"够陡(1.4×)"，也算"有新拨的迹象"。
**基频区那一把锁是必须的**（见 §1.4）。

### 1.4 中间版本翻过的车：跳音（重要）

第一版只写了"2kHz+ ≥ 2.0×"，结果**同一次拨弦被算两次**——高频带在衰减途中
还会抖出 ≥2 倍的小抬头（拾音噪声、拍频）。"等我弹"里每多一次起音就往前吃一个音，
用户看到的就是「**很多跳音**」。

真机实测（`vc_gf/onset-variants.mjs`）：

| 方案 | 6415慢速 | 6415快速 | hey_jude |
|---|---|---|---|
| 改前（无频带判据） | 51 次／重复 0 对 | 47／0 | 39／3 |
| 只 ≥2.0×（出问题那版） | 68／**6 对** | 62／**4** | 41／4 |
| **≥2.5× + 基频区 ≥1.2×（现在）** | 60／**0** | 56／**0** | 40／3 |

（"重复"= 相邻起音间隔 <250ms 的对数；覆盖真拨弦 13/18、9/10、28/33，
改前分别是 11/18、6/10、27/33。hey_jude 那 3 对是真快音，改前也是 3。）

### 1.5 标定与验收工具（都在 `C:\Users\Administrator\vc_gf\`）

| 脚本 | 干什么 |
|---|---|
| `band-attack.mjs` | 用"1.5~4kHz 30ms 内涨 2 倍"独立数真拨弦，再问起音层认没认 |
| `onset-variants.mjs` | 扫阈值/加锁方案的对照表（上面那张表就是它出的） |
| `after-detail.mjs` | 列出改前/改后每次起音（时刻、间隔、音、电平、频带抬头） |
| `band-env.mjs` | 打印频带包络，用来分辨"真新拨"和"余响上的小鼓包" |

---

## 2. 技巧：击弦 / 勾弦 / 滑音（一次起音、两个音）

用户口径：「滑音击弦还有勾弦，这里就是要判断两个不同的音但是只有一次起音」。

### 2.1 做法

- 谱面第二个音带 `tech: 'hammer' | 'pull' | 'slide'`（前端 `tech_practice.json` 已示范）；
- `advanceNote()` 里排一个 **合成的起音时刻** `techDueMs = onsetAtMs + clamp(谱面间距, 下限, 700ms)`，
  到点就当"一次起音"走**完全相同的判定链**，不要求真的再拨一下；
- 采样下限按真机实测落地时间定：**击弦/勾弦 250ms、滑音 520ms**
  （实测落地：击弦 ~170ms；滑音 ~270~420ms，滑音落地前是连续爬音，采早了会判成错音）；
- 真起音先到就取消 `techDueMs`（**谁先到算谁的**，避免同一个音判两次）。

### 2.2 判定按"音程"比，不按绝对音高

技巧的两个音在同一根弦上，琴整体偏高/偏低会一起平移：

```
真机实测：滑音 E4(335Hz) → 落点 298Hz
  按绝对音高比 → 读成 D#4 → 判错
  按音程算期望（335 × 2^(-2/12) = 297.7Hz）→ 差 2 音分 → 判对
```

实现：`techRefHz` = 上一个音**实测频率**（用 Hz，不用音分读数——同一次演奏里两个
一样的音频率只差 1Hz，但拟合出的音分能差 85）；期望 = `techRefHz × 2^(Δ半音/12)`；
容忍 `45 + CFG.techExtraCents(40) = 85 音分`（< 半音 100）。

### 2.3 验收（用页面真实代码 + 用户录音）

| 技巧 | 结果 |
|---|---|
| 滑音 2弦3品→5品 | 对 4 ／ 错 0 |
| 滑音 2弦5品→3品 | 对 4 ／ 错 0 |
| 击弦 1弦3品→5品 | 对 3 ／ 错 0 |
| 勾弦 1弦5品→3品 | 对 2 ／ 错 0 |

跑法（`test-follow-real.mjs` + 自定义时间轴，时间轴在 `vc_gf/tl-*.json`）：

```bat
set VC_TIMELINE=C:\Users\Administrator\vc_gf\tl-hammer.json
node test\test-follow-real.mjs "C:\Users\Administrator\vc_gf\T-hammer.f32"
```

> 为什么要 `T-*.f32`：脚本从 0 秒开始放录音，而页面开头有 4 拍倒计时，
> 直接喂原录音会把开头几秒吃掉、还会触发"检测到外放"。所以先把录音裁成
> **前 3.3 秒静音 + 目标片段**（生成方法见 `vc_gf` 里的裁剪命令）。

页面入口：曲目下拉新增「**技巧练习（击弦 / 勾弦 / 滑音）**」，
数据在 `frontend/data/tech_practice.json`（songKind = `tech`）。

---

## 3. 手机白屏/死按钮：两件事，都别忘

### 3.1 模块地址必须带版本号（真隐患，已修）

症状：**顶栏出来了、谱面空白、按钮全点不动**。
原因：手机缓存了**旧的 engine 模块**，新的 `follow-score.js` 一导入就报
`does not provide an export named 'hfBandRiseOf'`，**整个模块脚本一行不执行**。

修法：所有 ES 模块地址都带 `?v=<BUILD>`——
入口 `index.html`、8 个前端 js 里的 engine 导入、6 个 `js/engine/*.js` 转接文件。

- 检查：`node test/check-build.mjs`（必须 ✅，现在 28 处地址一致）
- 改版本号：`node C:\Users\Administrator\vc_gf\bump-build.mjs 0922-2130`
  （一键改 BUILD + 所有 `?v=`，改完跑一次 check-build）

### 3.2 启动自检红条（新增）

`index.html` 里加了一段**普通脚本**（模块之前跑）：捕获 `error` /
`unhandledrejection` / 5 秒还没启动 → 页面顶部红条直接写出原因和当前地址；
模块用 `import()` 拿，失败时能拿到具体错误。
验证方式：`vc_gf/stalecheck.py`（拦截 `analysis.js` 返回旧版）→ 红条显示
`模块加载失败：The requested module ... does not provide an export named ...`。

### 3.3 证书（不是今天白屏的主因，但顺手修了）

- 事实：服务器证书 SAN 写死 `192.168.0.81`（9/18 签），电脑今天变成 `192.168.0.131`；
- 但用户说「IP 天天变、天天都能用」→ 华为浏览器平时对证书是宽容的，
  **所以证书不是今天白屏的原因**，别再往这条路上归因；
- 仍然做了：`backend/renew-server-cert.ps1` —— **沿用原根证书**只重签叶子，
  SAN 用真正的 `IPAddress=` 型（不是把 IP 塞进 `DNS=`）。手机不用重装根证书。
  已重签 + 重启服务（当时 PID 13852）。
- ⚠ 这个 .ps1 **必须纯 ASCII**：Windows PowerShell 5.1 读没有 BOM 的 .ps1 按 GBK，
  中文注释会把脚本读坏（今天踩了一次）。

---

## 4. 其他小改动

- **切曲目先停跟弹**：`song.onchange` 里先 `stopMic()` + `phase='idle'` + `techDueMs=0`。
  以前跟弹进行中切曲目，麦克风循环还在跑而判定清单已换 → 用户报「切一下就卡」。
- `CFG.techExtraCents` 25 → **40**（技巧额外容忍；85 音分仍小于一个半音）。
- `frontend/test/practice.html` 的入口脚本也加了 `?v=`。

---

## 5. 已知未解决 / 别忘

1. **测量层"锚在期望音上"**（老毛病，今天又撞到）：同一段音频，
   期望 D4 时读 **297Hz**、期望 D#4 时读 **310Hz** —— 所以"故意弹错半音"的负例
   在高把位轻音上测不准。根治要换**不看答案的差分测量**（记忆同步里记过）。
2. **一弦天生轻**：导出记录按弦统计，一弦中位电平 **0.077**，
   二/三/四弦是 0.129 / 0.146 / 0.205，而门限 0.05 → 一弦是最先被切掉的。
   目前靠"频带抬头"缓解，**还没**给一弦单独放阈值。
3. `test-bughunt.mjs` **1 项老失败**：击弦落点 F3 应判过（合成场景，改前就失败）。
4. **和弦测试页 `chord_arp` 与用户实际弹法对不齐**（当时离线跑"对 11/错 21"），没解决。
5. **跟节拍（tempo）模式没接技巧路径**：`techDue` 只在"等我弹"生效。
6. 华为自带浏览器：对自签 HTTPS / JS 的处理可疑（同一地址百度浏览器正常）。
   若用户继续用它，先查它的 **JavaScript 开关**和**省流/无图模式**。

---

## 6. 常用命令

```bat
cd /d E:\GuitarFollowLab
node test\test-follow-page.mjs          :: 主回归（光标 + 判定 + 跟节拍）
node test\gt-notes.mjs                  :: 真机两向 39/39（判定底线）
node test\test-detect.mjs               :: 含"完全不消音 + 手机麦克风 T3231323"
node test\test-live.mjs                 :: 实时链路 + 极端延续
node test\check-build.mjs               :: 版本号一致性（必须 ✅）
node test\test-bughunt.mjs              :: 已知 1 项老失败
```

排查用（`C:\Users\Administrator\vc_gf\`）：

```bat
:: 真浏览器加载页面（真实 Chromium/playwright，能看 console / 失败请求 / 红条）
set PYTHONPATH=E:\Lib\site-packages
set PLAYWRIGHT_BROWSERS_PATH=E:\Lib\ms-playwright
E:\Python\python.exe -X utf8 pagecheck.py "https://192.168.0.131:1210/"

:: 曲目下拉逐项切一遍（看会不会卡）
E:\Python\python.exe -X utf8 switchcheck.py
```

服务端自报（手机该用哪个地址）：`http://localhost:1209/cert-info` → `lanIPs`。

---

## 7. 工作区未提交的改动（今天全部）

```
 M backend/engine/analysis.js      ← hfBandRiseOf / lowBandRiseOf（新测量）
 M backend/engine/onset.js         ← hfBandRiseMin 2.5 + lowBandRiseMin 1.2
 M backend/engine/config.js        ← techExtraCents 25→40
 M frontend/index.html             ← 启动自检红条 + 入口脚本带版本号 + 技巧练习选项
 M frontend/js/follow-score.js     ← 起音新量 / 技巧路径 / 切曲目停跟弹 / BUILD
 M frontend/js/engine/*.js (6)     ← 转接导出带 ?v=
 M frontend/js/*.js (其它 7)        ← engine 导入带 ?v=
 M frontend/test/practice.html     ← 入口脚本带 ?v=
?? backend/renew-server-cert.ps1   ← 换 IP 后重签证书（沿用原根证书）
?? frontend/data/tech_practice.json← 技巧练习样例（击弦/勾弦/滑音各一对）
?? test/check-build.mjs            ← 版本号一致性检查
```

> 提交时记得：这三样（`renew-server-cert.ps1`、`tech_practice.json`、`check-build.mjs`）
> 是新文件，别漏 `git add`。
