# 《思路.md》点评：哪些对得上、哪些要改、哪些是阈值问题

> 2026-09-21。对照：`调研-智能跟弹-竞品与开源对标-2026-09-20.md`、`跟弹-复盘与重新设计.md`、
> 以及今天在**你自己那段 30 秒录音**上量出来的数字。
> 一句话：**四条思路里，第 2 条（只看峰值瞬间）方向对但用错了地方，其余三条基本成立。**

## 0. 先说结论

| 你的思路 | 判断 | 一句话理由 |
|---|---|---|
| 练习模式：用户控速、等他弹、别把杂音算成音符 | ✅ 对得上 | 与竞品（Yousician 的 wait、Rocksmith 的 riff repeater）和 9-20 复盘里"光标=指令"一致 |
| 一、延音线当一个音、光标自动跳、窗口放宽 | ✅ 对得上 | 就是 MuseScore/TuxGuitar 的谱面语义；`gp_timeline.py` 加上 tie 标志即可 |
| 二、**只看峰值瞬间判音高** | ⚠️ **一半对** | 用峰值**分段**对；用峰值**量音高**错 —— 今天量到它把干净的 D4 读歪 70 音分 |
| 三、和弦逐音判定、能标出"3-4 错了" | ✅ 方向对，但有前提 | 要先有"按弦分带"这一层；现在是和弦外音判据，判不到"哪根弦没响" |
| 四、扫弦分辨不了节奏型就不做 | ✅ 对得上 | Yousician 那篇论文做的也是"歌→谱"的内容侧转写，不是实时判定 |
| 检查模式：按原速、不等、倒数、每音一个等待窗 | ⚠️ 能用但是近似 | 界面上没有"每音一个固定窗"的标准公式；学术/工业界用的是**跟踪器**（OTW/HMM/粒子滤波） |

## 1. 要指正的一条：峰值瞬间该用来"分段"，不该用来"量音高"

**你的原话**："在时间内我们只看音频内容里面'蹦出来'的那个音……看的是这段时间内峰值的那个音，
而不是在减弱的音。"

**对的一半**：用峰值决定"这一个音属于哪个时间窗"，以及"在衰减的音不管它"。这正是现在
`novelSpectrum` + 抬头过滤在做的事，是对的，也是继续要用的。

**错的一半**：拿"峰值那一瞬"去量音高。今天用你的录音量了同一件事：

| 在哪段音频上量 | 干净 D4 量出来 | 偏差 |
|---|---|---|
| 起音那一刻往回 170ms（= 现在的"峰值快照"） | 306Hz | **+70 音分** |
| 判定这一刻（起音后 90ms）往回 170ms | 295Hz | **+5 音分** |

为什么：起音那一刻往回 170ms 的窗**结束在起音那一瞬**，里面几乎全是上一个音的余响，
只有最后十几毫秒是新音。起振瞬间本身又是宽带瞬态、谐波还没立起来 —— **音高在那一瞬本来就测不准**。

**文献对得上**：Onsets and Frames（Hawthorne 2018）、Basic Pitch 都是"起音分支 + 持续段分支"两路，
音高主要靠持续段；业界没有拿单帧起振判音高的。

**改法**：起音那一刻只记"这里有一根弦被拨响"（分段）；音高在**起音后 30~170ms 的稳定段**量。
今天已经按这条做了，误差中位 45 → 16 音分（见 `test/probe-estimator.mjs`）。

## 2. 练习模式里最危险的不是杂音，是"插入错误"

你说"重点是不能把周围的杂音算成一个音符"——对，但**后果**比听起来严重：现在是"一响就吃掉一个
音符槽"，环境里响一声、光标往前一格，**后面整段对号全部错位**（用户会看到一片红）。

这不是阈值能解决的，是 score following 里的 insertion 问题：标准解法是加一层"位置跟踪"
（在线 DTW / HMM，带插入、删除代价），而不是"响一次就前进一个槽"。

短期能做的折中：起音必须**同时**满足"像一根弦在振动"（周期性强、谐波成串）才消耗音符槽；
不像就只当噪声、不消耗。现在有一道 `clarity > 0.42` 的关卡，但阈值没在真机噪声上调过
（风扇/说话的场景一次都没测过）。

## 3. 检查模式："必须有节奏才算弹对"—— 这是口径变更，要显式分开

现在的口径（记忆同步 9-20）是"不做节奏，只回答这一个音是不是谱面要的答案"。
你现在要的检查模式把**时间**也算进"对"里 —— 两套模式的"对"不是同一个意思，评分不能混着看。

建议：检查模式的输出**分成两项**（音准对错 / 时间偏差毫秒），别合成一个"对/错"——
否则用户被标红时不知道自己错在音还是错在拍。

### 关于"有没有现成的等待窗公式"

诚实回答：**没有一个行业标准公式**。公开的实现都是跟踪器，不是"每音一个固定窗"：

| 做法 | 出处 | 能学什么 |
|---|---|---|
| 在线时间规整 OTW（`c=300, max_run_count=3, diag_weight=0.4`） | web-score-following（MIT，WAC 2024） | 浏览器里能跑的最小跟踪器，参数可以照抄起步 |
| 粒子滤波谱面跟随 | Raphael 2006 | 速度连续变化的建模 |
| 工业级 | IRCAM Antescofo | 商业化实现 |

能当**起点**的经验值（不是标准，是够用的近似）：

- 容许窗按音符时值取比例：**±0.2~0.3 × 时值**，再夹一个下限和上限；
- 下限 **60ms** 左右（等时序列的起音差异阈约 20~30ms，60ms 已经很宽）；
- 上限 **250ms**（再宽就拖到下一个音了）；
- **第一个音给 2 倍**（你这条想法是对的，大家也这么做）；
- 倒数 4 拍按 BPM 走（你已经定了，和现在 `countIn` 一致）。

但要知道：固定窗在**速度变化、中途停手**时必崩。所以先出"偏差多少毫秒、错了几处"，
别急着判"对/错"。

## 4. 和弦逐音判定：方向对，但它是一层独立的工作

"和弦谱本质上也是数字谱，逐音判，能标出 3-4 这种错误"—— 对，而且这是竞品的做法
（Yousician 那类会给到具体音）。但有个前提：

**得先知道"每个音在哪根弦、哪一品、响了没有"**，也就是调研文档里列为 P2 的**按弦分带跟踪**。
现在判和弦用的是 chroma + 余弦（判"有没有和弦外音"），它天生回答不了"哪根弦没响/哪根弦多响了"。

所以顺序是：先把"六根弦各自的频带 + 谐波能量"做出来（谱面已经给了弦和品，是闭集问题），
再用它做逐音判定。不是改个参数的事。

## 5. 放在第二阶段的两条

- **"延音过早断掉算错"**：需要包络跟踪，而且"自然衰减"和"你按住弦止音"很难分（细弦本来衰减就快）。
  建议音准稳了之后再做。
- **扫弦**：同意"分不出节奏型就不判"。另外你要的"扫弦音频图"我给不了静态图的替代方案之前，
  先给个更省事的：`node test/probe-settle.mjs <录音.f32> <时刻> <期望MIDI>` 可以把任意时刻前后
  的音高轨迹打出来。要频图我可以另做一个脚本（能画，但要再花一点时间）。

## 6. 阈值与参数：今天能定下来的（都有实测依据）

| 参数 | 值 | 依据 |
|---|---|---|
| 量音高用的窗 | 起音后 ~90ms 起、往回取 170ms | 换窗位置这一条就把误差从 ±80 压到 ±40 音分 |
| 音高估计器 | 谐波峰 + 抛物线插值 + 多谐波最小二乘 + 弦刚性 | 中位误差 45 → 16 音分；±50 音分内 53% → 98% |
| 判"一品之差"的前提 | 测量散布 ≤ ±50 音分 | 一个半音 = 100 音分，测量必须比它准一倍 |
| 按弦音准基线 | 同弦中位数（≥5 样本）向全局中位数收缩一半 | 沿用 9-20 的结论 |
| 起音门限 | 噪声地板 × 4，且电平较 48ms 前抬升 1.15× | 沿用 |
| 重复音 | 不要求新音高，峰值对得上就放行 | 沿用 |

**还没定/定不了的**：检查模式的窗口公式（见 3）、插入错误的判定门槛（见 2）、
逐音和弦的按弦分带参数（见 4）。

## 7. 今天顺手修掉的一个硬 bug：**第一个音被吃掉**

`follow-score.js` 里有一句遗留的对齐逻辑，会把**第一个起音**当参考、直接 `return` 不判它。
你确认了开头那个 2弦1品 不是多余的声音 —— 那就是 bug，已经去掉：现在第一个音（4.85s 那一下）
会被判，谱面从第 1 个音开始对号。

## 8. 还没解决的：一品之差接不上判定（附证据）

按你的要求，判据必须能说"你按错一品了"。今天走到了哪一步：

1. 做出了一把准得多的尺子（`estimateF0ByPeaks`，上面第 1 节的数据）；
2. **把它接到判定上，你那 30 秒录音里有 9 处被读成"整整低一个半音"**（-88 ~ -125 音分）；
3. 其中至少一处是**尺子自己的错**：23.3s 那个音谱面要 G3，那段谱里最强的那根谱线就是
   199.2Hz（G3），尺子却给出 184Hz（F#3）—— 上一个音还在响、而两个音差一个全音时，
   谐波梳子会**锁到两者中间**。
4. 所以：判错会冤枉人，判定暂时回到今早那套（你的录音 对35/错1/测不准4）。

**要你做的事**：这 9 处我列了时间点，你听一下到底是你按低了还是我量歪了 ——
这决定下一步是修尺子还是改判据：

```
6.21s  A3(3弦2品)  → 读到 208Hz (G#3)
12.81s F4(1弦1品)  → 读到 325Hz (E4)
16.65s A#3(3弦3品) → 读到 219Hz (A3)
17.24s C4(2弦1品)  → 读到 245Hz (B3)
19.21s G4(1弦3品)  → 读到 378Hz (F#4)
20.29s F4(1弦1品)  → 读到 327Hz (E4)
21.33s D4(2弦3品)  → 读到 275Hz (C#4)
23.33s G3(3弦0品)  → 读到 184Hz (F#3)  ← 这一处基本可以确定是尺子错（谱里明明是 G3）
26.18s A#3(3弦3品) → 读到 222Hz (A3)
```

## 9. 下一步（按价值排）

1. **修精测尺子的"锁到两个音中间"**（换更长的窗 / 要求基频峰与拟合值一致 / 多窗取中位里选
   拟合最稳的那一个），修好后把它接到判定上 —— 这一步做完，一品之差才真的能判。
2. 按你的思路一：时间轴里加 **tie（延音线）**，光标跳过被延音的那一格、窗放宽。
3. 练习模式的**插入错误**：起音要"像一根弦"才消耗音符槽；拿风扇/说话声做一次真机测试。
4. 检查模式：先把"时间偏差 ms"和"错了几处"做出来，不做"对/错"二值。
5. 逐音和弦：先做按弦分带，再谈逐音判定。
