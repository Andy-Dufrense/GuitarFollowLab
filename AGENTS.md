# 铁律（跟弹项目 · 每次开工先读这一页）

> ⚠ **新窗口第一件事**：先读 `记忆同步-2026-09-23-夜-收工状态.md`
> （今天最完美的状态 / 用户原话思路 / 今天骂过的错 / 每个方向的下一步都在里面）。
> 不许从零试、不许夹带、不许擅自改用户没点名的地方。

> 这些是用户反复强调过的规矩。**每次新会话、每个新项目都要先读**，
> 不要等用户再骂一遍。违反任何一条，都算事故。

## 一、阈值与调参（用户 2026-09-23 明确定）

1. **不许大步跳**。调阈值一次只动**一个量**，幅度要小：
   以 0.05~0.5 为粒度去试（例如 0.3 → 0.35 → 0.41），
   **不要**从 0.4 一把跳到 3、也不要一次改好几个参数。
2. 每档都留数据：改了哪个值、那一档的**通过/误判数字**是多少，列成表给用户看，
   让他挑那一档，而不是替他挑。
3. 阈值**只写在 `backend/engine/`**（`config.js` 的 CFG / `onset.js` 的 ONSET），
   页面里不许散落魔法数字。

## 二、起音层是地基（用户原话："我跟你说过多少次起音的重要性"）

4. **没有离线验证，不许动起音层**。动之前先用用户的真机录音跑出数字，
   动之后必须给出"真音没丢、杂音没进"的两边数字。
5. **判据能通用，适用条件不能通用**。在"单旋律"上成立的判据（YIN 的 clarity 就是这样）
   挂到"多根弦一起响"的路径上**必然出事** —— 今天（9-23）就是把它通用到分解和弦，
   结果逐音全被丢。
6. **不许用"能自证"的判据**：锚在期望音上的读数、`nHarm ≥ 3`、单纯的"2kHz 抬头"——
   这些噪声/敲桌子/邻音也能满足；今天用它们做 1 弦放宽，结果是**敲桌子不停判对**。
7. 结构上以用户 9-21 的思路为准（`思路点评-2026-09-21.md`）：
   **起音分支只判"这一下有没有一根弦被拨响"；音高在起音后 30~170ms 的稳定段量；
   "像一根弦（周期性强 + 谐波成串）"加在消耗音符槽那一步。**

## 三、碰代码的边界

8. **只动用户点名的东西**。没点名的模块（光标 `cursor.js`、起音层参数、判定阈值……）
   一律不许"顺手改"。今天擅自改光标，两次都改坏（乱跳），用户明确说"我让你动光标了？"
9. **特例不许按歌名/测试页写**。要通用就真的通用（所有谱子同一条链）；
   但不能为了"通用"把不成立的判据硬挂上去。
10. **改完必须报**：改了哪个文件、哪个函数、动了哪个值、跑了哪些回归、数字是多少。
    过不了就说"没过"，不许含糊成"应该没问题"。

## 四、沟通

11. 报告**先给结论和数字**，再讲原因；不要用"应该""大概""可能"糊过去。
12. 不确定就问一句，不要自己替用户做产品决定（例如"把绝对门限从 0.05 抬到 0.08"这种
    会改变他弹琴力度的取舍，必须他自己定）。
13. 用户说过"什么是有用的改动、什么要撤"之后，按他的清单执行，不要再夹带。

## 五、回归底线（每次改完都要跑）

```bat
cd /d E:\GuitarFollowLab
node test\gt-notes.mjs          :: 39 个真机拨弦双向 39/39（判定底线）
node test\check-build.mjs       :: 版本号一致（必须 ✅）
node test\test-follow-page.mjs  :: 主回归（跟节拍有 4 条老口径断言待改，见 9-23 记忆）
E:\Python\python.exe -X utf8 C:\Users\Administrator\vc_gf\voice-gate.mjs <f32> [标签]
                                :: 起音层 + 各把尺子的分布表（DIST 那行）
```

杂音三段（必须"一下都不算"）：`sound_data\说话.m4a`、`咳嗽.m4a`、`周围有人弹吉他.m4a`
（后两段电平 0.051~0.079，贴着 0.05 的绝对线）。
